# Daily Adversarial Review - 2026-07-01

## 审查时间

- 运行时间: 2026-07-01 10:16:31 +08:00 (Asia/Shanghai)
- Automation ID: `agent-2`
- 审查模式: 多 Agent 对抗性只读审查；允许写入本报告；未修改源码、未格式化、未提交、未清理工作区

## 分支

- 当前分支: `feature/workflow-workstation-alignment-dev`
- 上游: `origin/feature/workflow-workstation-alignment-dev`
- 上游差异: `ahead 1`
- ahead commit: `19c0e8b7 docs(project): refine local artifact ignore rules`

## 工作区状态摘要

`git status --short --branch`:

```text
## feature/workflow-workstation-alignment-dev...origin/feature/workflow-workstation-alignment-dev [ahead 1]
?? src/
```

未跟踪文件:

```text
src/types/import/auto-imports.d.ts
src/types/import/components.d.ts
```

相对上游 diff:

```text
.gitignore | 10 +++++++++-
```

`git diff --name-status --find-renames '@{u}...HEAD'`:

```text
M	.gitignore
```

CodeGraph: `.codegraph/` 存在，索引 up to date；已优先使用 CodeGraph 确认 `tests/` 是项目索引内真实测试结构。

## 审查范围

- 当前工作区未跟踪文件: `src/types/import/auto-imports.d.ts`, `src/types/import/components.d.ts`
- 当前分支相对上游的唯一提交: `.gitignore`
- 重点规则: `AGENTS.md`, `PROJECT_STATE.md`, `ARCHITECTURE.md`, `QUICKSTART.md`, `CODING_RULES.md`, `FRONTEND_RULES.md`, `DYNAMIC_WORKFLOW_RULES.md`, `GIT_RULES.md`, `TASK_LIFECYCLE_RULES.md`, `TASK_MANAGEMENT_RULES.md`
- 后端对照: 本次 diff 未修改 API、权限、患者/报告字段、DB 或业务规则；未触发 sibling `../SYBaseProject` 代码对照。

## Findings

### Critical

未发现可证实问题。

### High

未发现可证实问题。

### Medium

#### 1. `/tests` 会隐藏未来新增测试

- 严重级别: Medium
- 文件与行号: `.gitignore:91`
- 证据:
  - `.gitignore:91` 新增 `/tests`
  - `git check-ignore -v tests/e2e/new-flow.spec.ts tests/e2e/.auth/new.json tests/reports/foo.txt` 均命中 `.gitignore:91:/tests`
  - `git ls-files 'tests/**'` 显示仓库已有 `tests/e2e/...`、`tests/e2e/smoke/...`、`tests/e2e/specimen-workflow/...` 等被跟踪测试
  - `playwright.config.ts:6` 使用 `testDir: './tests/e2e'`
  - `docs/rules/FRONTEND_RULES.md:46` 规定 E2E 位于 `tests/e2e/smoke/` 和业务子目录
- 影响:
  - 新增 E2E、helper、setup 或测试报告相关审查产物可能不会出现在普通 `git status` 中。
  - 对权限、患者报告、导出、审计等 Full/Red Team 场景，新增回归测试可能被本地忽略，削弱验证证据链。
- 建议修复:
  - 删除根级 `/tests`，保留已有精确规则，例如 `tests/reports/`、`tests/e2e/.auth/`、`test-results/`、`playwright-report/`。
  - 如确需忽略临时测试输出，限定到具体产物目录，不忽略测试源目录。
- 建议验证:
  - `git check-ignore -v tests/e2e/smoke/new-smoke.spec.ts` 应不再命中。
  - `git check-ignore -v tests/e2e/.auth/new.json tests/reports/foo.txt` 应继续命中预期产物规则。

#### 2. `/docs/user-manual` 会隐藏未来新增手册正文

- 严重级别: Medium
- 文件与行号: `.gitignore:92`
- 证据:
  - `.gitignore:92` 新增 `/docs/user-manual`
  - `git check-ignore -v docs/user-manual/new-page.md docs/user-manual/images/new.png docs/user-manual/.generated/new.json` 均命中 `.gitignore:92:/docs/user-manual`
  - `git ls-files 'docs/user-manual/**'` 显示 `docs/user-manual/README.md`、`dashboard.md`、`m1-system.md` 到 `m6-statistics.md` 等正文已被跟踪
  - `docs/user-manual/README.md` 与 `scripts/generate-user-manual.mjs` 表明正文 Markdown 是正式交付物，截图/manifest/log 才是可再生成产物
- 影响:
  - 未来新增模块手册 Markdown 可能被忽略，造成手册交付、验收证据与实际工作区不一致。
  - 当前已有更精确规则已覆盖 `images/`、capture manifest、capture log；整目录忽略扩大了边界。
- 建议修复:
  - 删除 `/docs/user-manual`。
  - 保留或补齐精确产物规则: `docs/user-manual/images/`, `docs/user-manual/.generated/`, `docs/user-manual/.capture-manifest.json`, `docs/user-manual/capture-errors.log`。
- 建议验证:
  - `git check-ignore -v docs/user-manual/new-module.md` 应不再命中。
  - `git check-ignore -v docs/user-manual/images/new.png docs/user-manual/.generated/new.json` 应继续命中。

#### 3. `/.codex` 覆盖了前面的 `.codex` 白名单

- 严重级别: Medium
- 文件与行号: `.gitignore:64`, `.gitignore:65`, `.gitignore:66`, `.gitignore:67`, `.gitignore:68`, `.gitignore:93`
- 证据:
  - `.gitignore:64-68` 先写入 `.codex/*` 并显式反向保留 `!.codex/hooks.json`, `!.codex/session-summary.project.json`, `!.codex/hooks/`, `!.codex/hooks/**`
  - `.gitignore:93` 后置新增 `/.codex`
  - `git check-ignore -v .codex/hooks.json .codex/session-summary.project.json .codex/hooks/test.ps1` 均命中 `.gitignore:93:/.codex`
- 影响:
  - 后置整目录忽略让前面白名单失效，未来 Codex hook、session summary 或自动化护栏配置可能被隐藏。
  - 如果这些文件承担审计、自动化门禁或团队配置作用，reviewer 无法从普通 `git status` 发现新增/变更。
- 建议修复:
  - 删除后置 `/.codex`，使用 `.codex/*` + 精确反向规则表达意图。
  - 如果 `.codex` 全部应本地忽略，则删除前面的白名单，避免规则自相矛盾；但需明确 hook 配置是否仍需入库。
- 建议验证:
  - `git check-ignore -v .codex/hooks.json .codex/session-summary.project.json .codex/hooks/test.ps1` 应符合最终意图。

### Low

#### 4. 当前未跟踪生成声明未被本次本地产物忽略规则覆盖

- 严重级别: Low
- 文件与行号: `src/types/import/auto-imports.d.ts:1`, `src/types/import/components.d.ts:1`
- 证据:
  - `git status --short --branch` 显示 `?? src/`
  - `git ls-files --others --exclude-standard` 显示 `src/types/import/auto-imports.d.ts` 和 `src/types/import/components.d.ts`
  - 文件头分别标记 `Generated by unplugin-auto-import` 与 `Generated by unplugin-vue-components`
  - `git check-ignore -v src/types/import/auto-imports.d.ts src/types/import/components.d.ts` 未命中
- 影响:
  - ahead commit 主题是 refined local artifact ignore rules，但当前实际遗留的生成声明仍暴露为未跟踪文件，规则与工作区事实不一致。
  - 不直接影响运行时，但会持续污染每日审查和开发者状态判断。
- 建议修复:
  - 确认这些声明应入库、移动到应用内预期路径，还是作为生成产物精确忽略。
  - 不建议用根级 `/src` 这类宽泛规则处理。
- 建议验证:
  - 根据决策运行 `git status --short --untracked-files=all`，确认只剩预期未跟踪文件。

## Red Team Notes

- Attack path: 开发者新增权限/患者报告/E2E 回归测试到 `tests/e2e/...`，或新增 Codex hook 到 `.codex/hooks/...`；由于 `.gitignore` 宽泛根规则，普通 `git status` 不显示新增文件，PR 可在缺少测试/护栏更新的情况下进入 review。
- Expected failure point: Git ignore 应只过滤可再生成产物，不能隐藏测试源、手册正文或显式白名单的自动化配置。
- Attack result:
  - `git check-ignore -v tests/e2e/new-flow.spec.ts` 命中 `.gitignore:91:/tests`
  - `git check-ignore -v docs/user-manual/new-page.md` 命中 `.gitignore:92:/docs/user-manual`
  - `git check-ignore -v .codex/hooks.json` 命中 `.gitignore:93:/.codex`
- Residual risk:
  - `.codex/hooks*` 是否为正式团队护栏仍需 owner 决策。
  - 未执行完整 lint/typecheck/E2E；本次只读审查聚焦当前 diff 和工作区事实。

## Test Gaps

- `pnpm run check:governance` 未完成: pnpm 在无 TTY 环境尝试 `install` 并因 `[ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY]` 中止。为避免依赖目录改写，未设置 `CI=true` 或 `confirmModulesPurge=false` 重跑。
- 已执行替代只读验证: `node .\scripts\validate-governance.mjs`，结果 `Governance validation passed.`
- 已执行 `git diff --check '@{u}...HEAD'`，结果 exit 0。
- 未执行 `pnpm lint`, `pnpm check:type`, `pnpm test:unit`, `pnpm test:e2e`，原因: 本次 diff 为 `.gitignore` 治理边界审查，且用户要求只读为主、不得运行会重写文件的 formatter/linter。

## No-Finding Areas

- 未发现 `.gitignore` diff 直接修改患者、报告、权限、认证、路由守卫、API service/mapper、全局请求层、导出或后端契约。
- 未发现新增 ignore 规则影响 `apps/web-ele/src/main.ts`, `apps/web-ele/package.json`, `apps/web-ele/vite.config.ts` 等前端运行入口。
- Vitest 单测发现未见直接影响: `vitest.config.ts` 已排除 `**/e2e/**`，当前 `/tests` 风险主要集中在 Playwright/E2E 和新增测试源可见性。
- `src/types/import/*.d.ts` 为生成声明，未见患者字段、报告生命周期、API endpoint 或权限码变更；且未被当前 `.gitignore` 忽略。

## Needs Evidence

- `.codex/hooks.json`, `.codex/session-summary.project.json`, `.codex/hooks/**` 是否应作为正式仓库护栏入库。如果是，`/.codex` 必须移除；如果不是，应删除前面的白名单并记录决策。
- `docs/user-manual/.generated/` 与 `docs/user-manual/images/` 的最终入库策略需要确认；正文 Markdown 与截图产物应分开表达。
- `src/types/import/*.d.ts` 的生成来源和期望归属需要确认；当前仓库搜索未发现显式 `unplugin-auto-import` / `unplugin-vue-components` 配置，可能来自外部/历史工具链。

## Follow-ups

1. 收窄 `.gitignore` 末尾三条宽泛规则，至少移除 `/tests`, `/docs/user-manual`, `/.codex` 或改为精确产物路径。
2. 补一组 `git check-ignore` 回归命令到治理验证或 PR checklist，覆盖测试源、手册正文、Codex 白名单和生成产物。
3. 决策 `src/types/import/*.d.ts` 是应删除、入库、迁移还是精确忽略；避免长期保留根目录未跟踪 `src/`。
4. 在可控环境重跑 canonical `pnpm run check:governance`，确认 pnpm wrapper 路径也通过。
