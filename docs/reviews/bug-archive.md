# Bug Archive

Resolved bugs archived from `docs/memory/KNOWN_BUGS.md` on 2026-06-18. IDs are preserved.

## Entry Format (full)

| ID | Status | Reproduction | Impact | Workaround | Verification |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| BUG-20260608-001 | Resolved | 染色出片工作站选中病例后，已完成出片表格的“切片操作”“出片操作”在无操作人/时间时会把后端追踪状态 `qualityStatus=CREATED`、`slideStatus=PENDING` 原样显示为英文。后端来源核对：`../SYBaseProject/bl-center/src/main/java/com/company/bl/application/service/TechnicalProcessingWorkflowService.java` 创建玻片时写入 `PENDING`/`CREATED`。 | 操作列出现英文枚举，影响工作站中文界面一致性和用户识读。 | 无需后端规避；前端展示层通过状态映射本地化。 | Fixed in `apps/web-ele/src/modules/technical-workflow/utils/format.ts` and covered by `apps/web-ele/src/modules/technical-workflow/views/StainingWorkstationView.test.ts`; `pnpm test:unit -- --run apps/web-ele/src/modules/technical-workflow/views/StainingWorkstationView.test.ts`, `pnpm check:type`, `pnpm lint` passed on 2026-06-08. |
| BUG-20260608-002 | Resolved | 诊断分片页选择左侧用户并勾选右侧待分派任务后点击“初步分片”，当任务本身尚无责任医生/审核医生时，前端提示“选中任务缺少接口必填医生字段，已全部跳过”。后端契约核对：`../SYBaseProject/bl-center/src/main/java/com/company/bl/interfaces/dto/AssignDiagnosticTaskRequest.java` 对责任诊断医生、初诊医生、审核医生三组字段均要求 `@NotBlank`。 | 空白待分派任务无法完成初步分片，批量操作被前端本地校验全部拦截。 | 无需后端规避；前端分片请求在保留已有医生的同时，用左侧选中医生补齐缺失必填医生字段。 | Fixed in `apps/web-ele/src/modules/doctor-workflow/views/DiagnosisAssignmentView.vue` and covered by `apps/web-ele/src/modules/doctor-workflow/views/doctor-workflow-visibility.test.ts`; `pnpm test:unit -- --run apps/web-ele/src/modules/doctor-workflow/views/doctor-workflow-visibility.test.ts`, `pnpm check:type`, `pnpm lint` passed on 2026-06-08. |
| BUG-20260608-003 | Resolved | 医生工作站中 HE 染色医嘱执行收费后，右侧医嘱区显示“已收费”，但常规医嘱工作站列表未显示该记录。后端核对：`../SYBaseProject/bl-center/src/main/resources/db/migration/V2__seed_m1_reference_data.sql` 中旧字典 `ODI_HE` 位于 `ODC_ROUTINE`，分类码为 `ROUTINE`；`../SYBaseProject/bl-center/src/main/resources/db/migration/V77__seed_medical_order_dictionary_catalog.sql` 中新字典 `ODI_CGRS_HE_STAIN` 位于 `CGRS`。前端常规工作站原查询 `EXAM,CGRS,BLOCK,QP` 漏掉旧分类 `ROUTINE`；后端 `JdbcMedicalOrderRepository` 默认查询 `PENDING/IN_PROGRESS`，收费只更新 `billing_status`，不应从常规待办消失。 | 旧字典“常规医嘱”下创建的 HE 染色医嘱被常规工作站查询参数排除，影响收费后执行确认。 | 无需后端规避；前端常规医嘱工作站同时查询 `ROUTINE,EXAM,CGRS,BLOCK,QP`，并展示 `billingStatus`。 | Fixed in `apps/web-ele/src/modules/technical-workflow/utils/medical-order-workstation.ts` and `apps/web-ele/src/modules/technical-workflow/utils/technical-order-workstations.ts`; covered by `apps/web-ele/src/modules/technical-workflow/utils/medical-order-workstation.test.ts` and `apps/web-ele/src/modules/technical-workflow/views/TechnicalOrderWorkstationsView.test.ts`; targeted vitest, `pnpm lint`, and `pnpm check:type` passed on 2026-06-08. |
| BUG-20260608-004 | Resolved | 常规医嘱工作站“医嘱时间”列显示不完整，秒级时间被前端映射截断或因列宽不足换行；其他医嘱页面的 `orderDate` 展示格式也不统一。 | 医嘱执行人员无法直接读取完整开单时间，跨页面查看同一医嘱时间时格式不一致。 | 无需后端规避；前端统一将 `orderDate` 展示为 `YYYY-MM-DD HH:mm:ss`，并加宽常规医嘱工作站“医嘱时间”列。 | Fixed in `apps/web-ele/src/modules/technical-workflow/utils/medical-order-workstation.ts`, `apps/web-ele/src/modules/technical-workflow/utils/technical-order-workstations.ts`, `apps/web-ele/src/modules/doctor-workflow/utils/format.ts`, and `apps/web-ele/src/modules/doctor-workflow/views/ReportTrackingView.vue`; covered by technical order workstation tests and doctor workflow visibility tests. |
| BUG-20260608-005 | Resolved | 诊断平台工作站医嘱区中，蜡块下拉列表和医嘱项目列表把当前病例病理号拼进蜡块标签，例如显示 `HE 染色（蜡块: BK20260525001 bb）`。 | 医嘱区重复展示病理号，干扰医生选择蜡块和查看医嘱项目。 | 无需后端规避；前端展示层清理当前病例病理号前缀，仅保留蜡块号/组织描述。 | Fixed in `apps/web-ele/src/modules/doctor-workflow/components/DiagnosisWorkbenchMedicalOrderPane.vue` and covered by `apps/web-ele/src/modules/doctor-workflow/views/DiagnosisWorkbenchView.test.ts`; `pnpm test:unit -- --run apps/web-ele/src/modules/doctor-workflow/views/DiagnosisWorkbenchView.test.ts`, targeted `pnpm exec eslint`, targeted `pnpm exec oxfmt --check`, and `pnpm check:type` passed on 2026-06-08. |
| BUG-20260608-006 | Resolved | 离体确认、标本固定、标本确认、标本入库、标本出库、标本接收工作台、取材描写工作站、脱水工作站只能看到当前待办状态；已离体、已固定、已确认、已入库、已接收、已完成取材/脱水等旧状态记录按标本号、申请单号、病理号或任务 ID 查询时不可回显。2026-06-17 回归反馈：脱水工作站点击“查询”按工作日期检索时未传 `includeAllStatuses=true`，已脱水完成记录仍被后端活动状态过滤排除。 | 用户无法通过业务编号复查历史状态数据，容易误判数据丢失；若强行回到旧列表语义又可能导致重复确认、重复入库、重复开始/完成。 | 前端普通进入保持空列表；显式查询走全状态数据展示，并沿用状态列、禁用原因和操作前状态校验；后端配合 `includeAllStatuses` 与出库显式条件查询。 | Fixed across `apps/web-ele/src/modules/specimen-workflow` and `apps/web-ele/src/modules/technical-workflow`; backend support in `../SYBaseProject/bl-center`. Covered by targeted Vitest for the 9 affected frontend files (73 tests), `pnpm check:type`, targeted lint checks, and backend targeted tests for technical task/outbound queries on 2026-06-08. 2026-06-17 regression fixed in `apps/web-ele/src/modules/technical-workflow/views/DehydrationWorkstationView.vue` by always sending `includeAllStatuses=true` for explicit dehydration workstation queries; covered by `DehydrationWorkstationView.test.ts`. |
| BUG-20260608-007 | Resolved | 标本采集工作台输入不存在的申请单号（例如 `1122`）后，先提示“申请登记工作台记录不存在”，随后自动创建申请单时报“请求体不能为空，且必须是合法的 JSON”。后端核对：`../SYBaseProject/bl-center/src/main/java/com/company/bl/interfaces/dto/CreateApplicationRequest.java` 的 `specimenRemovalTime` 为 `LocalDateTime`，全局 `HttpMessageNotReadableException` 会统一映射为该 JSON 错误文案。 | 申请单号未命中时无法自动创建新申请单，采集人员不能进入仿真患者/申请信息草稿。 | 无需后端规避；前端创建申请单 payload 将离体时间发送为后端可解析的 ISO `YYYY-MM-DDTHH:mm:ss`，工作台保存 payload 仍保留页面现有 `YYYY-MM-DD HH:mm:ss` 展示格式。 | Fixed in `apps/web-ele/src/modules/specimen-workflow/utils/application-registration-auto-create.ts`; covered by `application-registration-auto-create.test.ts`, `ApplicationRegistrationWorkbenchPanel.test.ts`, `application-registration-workbench-service.test.ts`, and `application-registration-workbench-mock.test.ts`; targeted Vitest, `pnpm check:type`, and `pnpm lint` passed on 2026-06-08. |
| BUG-20260608-008 | Resolved | 标本采集工作台输入不存在的申请单号后，lookup 404 被页面用于自动创建，但全局请求层先提示“申请登记工作台记录不存在”；随后自动创建申请单把展示年龄（如 `35岁` / `35岁0月0天`）作为 `patientAge` 发给 `POST /api/v1/applications`，后端达梦环境插入 `patients.age` 发生类型转换异常并返回 500。 | 申请单号未命中时仍无法自动创建并回显申请单，页面出现误导性的 404 与 500 提示。 | 前端自动创建申请单 payload 仅发送纯数字年龄，工作台患者信息仍保留展示年龄；lookup 请求设置 `skipErrorMessage`，让 404 未命中静默进入自动创建并直接回显数据，不再弹“申请单不存在，已自动创建并生成仿真申请信息”；后端 `../SYBaseProject/bl-center/src/main/java/com/company/bl/application/service/ApplicationPatientIdentityResolver.java` 兜底提取展示年龄开头数字后入患者档案。 | Fixed in `apps/web-ele/src/modules/specimen-workflow/utils/application-registration-auto-create.ts` and `apps/web-ele/src/modules/specimen-workflow/api/application-registration-workbench-service.ts`; backend fixed in `../SYBaseProject/bl-center`. Frontend `pnpm vitest run --dom --run apps/web-ele/src/modules/specimen-workflow/utils/application-registration-auto-create.test.ts apps/web-ele/src/modules/specimen-workflow/api/application-registration-workbench-service.test.ts apps/web-ele/src/modules/specimen-workflow/components/ApplicationRegistrationWorkbenchPanel.test.ts`, `pnpm check:type`, and `pnpm lint` passed on 2026-06-08; backend targeted `ApplicationCrudAndWorkflowLockIntegrationTest#shouldNormalizeDisplayAgeWhenAutoCreatingPatient` passed. |
| BUG-20260608-009 | Resolved | 条码绑定页面默认勾选“仅显示未绑定”，进入页面或重置后会向 `GET /api/v1/specimens` 发送 `barcodeBindingStatus=UNBOUND`，截图中列表无法加载已绑定等全部标本信息。后端核对：`../SYBaseProject/bl-center/src/main/java/com/company/bl/infrastructure/persistence/JdbcSpecimenWorkflowManagementProjectionSupport.java` 仅在传入 `UNBOUND`/`BOUND` 时追加条码绑定过滤，不传该参数即返回全部标本管理行。 | 用户在条码绑定工作台无法直接查看全部标本信息，统计和导出也跟随未绑定筛选收窄。 | 无需后端规避；前端默认和重置状态改为不启用未绑定筛选，用户手动勾选“仅显示未绑定”时才发送 `barcodeBindingStatus=UNBOUND`。 | Fixed in `apps/web-ele/src/modules/specimen-workflow/composables/useSpecimenBarcodeBindingPanel.ts`; covered by `useSpecimenBarcodeBindingPanel.test.ts` and `SpecimenBarcodeBindingPanel.test.ts`; `pnpm test:unit -- --run apps/web-ele/src/modules/specimen-workflow/composables/useSpecimenBarcodeBindingPanel.test.ts apps/web-ele/src/modules/specimen-workflow/components/SpecimenBarcodeBindingPanel.test.ts`, `pnpm check:type`, and `pnpm lint` passed on 2026-06-08. |
| BUG-20260609-001 | Resolved | 标本入库或标本转运页面已经默认选中当前登录人为操作人，点击“入库”或“转运”时仍提示“请选择操作人”或需要二次操作人确认。后端核对：`../SYBaseProject/bl-center` 原 check-in / transport DTO 对 `operatorVerificationToken` 要求必填，且控制器总是解析确认 token。 | 当前登录人不能直接作为入库/转运操作人完成操作，影响 M2 标本入库和转运出库效率。 | 前端当前登录人路径跳过二次确认并省略 `operatorVerificationToken`；非当前登录人仍调用原操作人确认；后端仅在请求操作人等于当前用户时允许 token 为空。 | Fixed in `useSpecimenCheckInPanel.ts`, `TransportHandoverView.vue`, and sibling backend `../SYBaseProject/bl-center` check-in / transport controllers and DTOs. Frontend targeted Vitest (4 files, 28 tests), `pnpm lint`, `pnpm check:type`, targeted `pnpm exec oxfmt --check`, and backend `SpecimenWorkflowClosureIntegrationTest` passed on 2026-06-09. |
| BUG-20260609-002 | Resolved | 标本出库页面扫描已完成出库/转运的标本时，后端 `GET /api/v1/specimen-outbounds` 可返回 `IN_TRANSIT` 或带 `outboundAt` 记录；页面应显示该记录用于确认，但不能再次勾选或转运。后端核对：`../SYBaseProject/bl-center/src/main/java/com/company/bl/application/service/SpecimenTransportService.java` 出库后会将标本和申请状态置为 `IN_TRANSIT`，相关集成测试也断言出库列表可返回 `outboundAt`。 | 若隐藏已转运标本，扫码后只看到“标本已完成出库，无需重复操作”提示和空表，用户无法确认扫到的是哪条数据。 | 无需后端规避；前端出库工作台继续展示 `OUTBOUNDED` 记录，沿用 `canOutbound=false` 使选择和转运保持禁用，扫码时继续提示“已完成出库，无需重复操作”。 | Fixed in `apps/web-ele/src/modules/specimen-workflow/utils/transport-handover.ts` and `apps/web-ele/src/modules/specimen-workflow/views/TransportHandoverView.vue`; covered by `transport-handover.test.ts` and `TransportHandoverView.test.ts`; targeted Vitest (2 files, 14 tests), `pnpm lint`, and `pnpm check:type` passed on 2026-06-09. |
| BUG-20260609-003 | Resolved | 标本接收工作台中接收成功的标本行使用页面局部深绿色整行样式，而标本确认页已确认行使用共享的浅绿色行状态样式。 | 同一 M2 标本流程中“已完成/已确认/已接收”状态的行色不一致，影响用户跨页面识别状态。 | 无需后端规避；前端接收工作台复用 `specimen-workflow-row-tone.css` 与 row tone class，`SUCCESS` / `RECEIVED` 映射为 `completed`。 | Fixed in `apps/web-ele/src/modules/specimen-workflow/views/SpecimenReceiptView.vue` and `apps/web-ele/src/modules/specimen-workflow/utils/specimen-workflow-row-tone.ts`; covered by `specimen-workflow-row-tone.test.ts` and `SpecimenReceiptView.test.ts`; targeted Vitest and touched-file format/ESLint passed on 2026-06-09. Full `pnpm lint` and `pnpm check:type` are blocked by unrelated dirty-worktree issues listed in delivery notes. |
| BUG-20260609-004 | Resolved | 条码绑定页显示的“标本条码”在未点击“条码绑定”前已经出现 `申请单号-标本编号`，例如截图中的 `1123-SP20260608017`。后端核对：`../SYBaseProject/bl-center/src/main/java/com/company/bl/application/service/SpecimenRegistrationService.java` 原登记逻辑在 `items[].barcode` 为空时自动生成 `applicationNo + "-" + specimenNo`，而管理列表用 `specimens.barcode` 非空判断 `BOUND`。 | 用户未输入目标条码、未点击条码绑定时，标本已经被当成已绑定，导致“未绑定 0”和条码绑定按钮不可用；标本条码列位置也不符合页面要求。 | 前端列顺序调整为“标本编号”后紧跟“标本条码”，mock 改为登记后未绑定；真实修复在后端 sibling 仓库：新登记未传条码时保存 `null`，绑定接口写入目标条码。 | Fixed in `apps/web-ele/src/modules/specimen-workflow/components/SpecimenBarcodeBindingPanel.vue`, `useSpecimenBarcodeBindingPanel.ts`, frontend mock/type files, and sibling backend `../SYBaseProject/bl-center`; frontend targeted Vitest (3 files, 22 tests), `pnpm check:type`, `pnpm lint`, and backend `M2CollectionAndLabelIntegrationTest` passed on 2026-06-09. |
| BUG-20260609-005 | Resolved | 取材描写工作站包埋盒区域的“标本名称”下拉选择器在 flex 头部内被压缩到只剩下拉箭头，选择“背部黑毛痣”等标本后控件内无法显示已选名称。 | 取材人员无法从控件本体确认当前包埋盒操作对应的标本名称，容易误操作。 | 无需后端规避；前端将该页面局部 `ElSelect` 固定为 `288px` 最小宽度并禁止 flex shrink，同时保留下拉弹层最小宽度。 | Fixed in `apps/web-ele/src/modules/technical-workflow/components/GrossingEmbeddingBoxTable.vue`; covered by `GrossingEmbeddingBoxTable.test.ts` and `GrossingWorkstationView.test.ts`; `pnpm test:unit -- apps/web-ele/src/modules/technical-workflow/components/GrossingEmbeddingBoxTable.test.ts apps/web-ele/src/modules/technical-workflow/views/GrossingWorkstationView.test.ts`, `pnpm lint`, and `pnpm check:type` passed on 2026-06-09. |
| BUG-20260609-006 | Resolved | 诊断分片页面筛选区的“任务类型”“任务状态”下拉选择器在 flex 表单内被压缩到只剩下拉箭头，无法正常显示已选筛选值。 | 分派人员难以确认当前任务类型/状态筛选条件，影响诊断分片列表筛选识读。 | 无需后端规避；前端将两个筛选 `ElSelect` 固定为 `176px` 最小宽度并禁止 flex shrink。 | Fixed in `apps/web-ele/src/modules/doctor-workflow/views/DiagnosisAssignmentView.vue`; covered by `doctor-workflow-visibility.test.ts`; `pnpm test:unit -- apps/web-ele/src/modules/doctor-workflow/views/doctor-workflow-visibility.test.ts`, `pnpm lint`, and `pnpm check:type` passed on 2026-06-09. |
| BUG-20260609-007 | Resolved | 取材描写工作站输入已完成取材的病理号/任务号并按回车查询时，列表返回历史任务后页面继续加载取材工作台上下文，顶部显示“技术任务未处于激活状态”。 | 用户查询历史取材记录时被错误提示干扰，且左侧列表缺少直接可见的数据状态。 | 无需后端规避；前端列表新增“数据状态”列，非 `IN_PROGRESS` 查询结果只保留列表/摘要展示并禁用取材操作，不再初始化取材工作台上下文。 | Fixed in `apps/web-ele/src/modules/technical-workflow/views/GrossingWorkstationView.vue`; covered by `GrossingWorkstationView.test.ts`; `pnpm test:unit -- --run apps/web-ele/src/modules/technical-workflow/views/GrossingWorkstationView.test.ts`, `pnpm lint`, and `pnpm check:type` passed on 2026-06-09. |
| BUG-20260609-008 | Resolved | 脱水工作站在病理号输入框扫码并按回车时只执行查询，不会把当前蜡块任务推进到开始脱水状态；用户还需要额外点击“开始脱水”。后端核对：`../SYBaseProject/bl-center/src/main/java/com/company/bl/interfaces/controller/DehydrationController.java` 的 `POST /api/v1/dehydrations/start` 会将选中脱水技术任务推进到 `IN_PROGRESS`，且 `TechnicalGrossingWorkflowService#startDehydration` 仅允许 `PENDING` 任务开始。 | 脱水人员使用扫码枪录入时无法一次回车完成开始脱水，影响工作站扫码效率。 | 无需后端规避；前端回车处理改为查询后自动开始当前/默认第一条 `PENDING` 任务，非待处理状态仍拦截，按钮批量开始行为保持不变。 | Fixed in `apps/web-ele/src/modules/technical-workflow/views/DehydrationWorkstationView.vue`; covered by `DehydrationWorkstationView.test.ts`; `pnpm test:unit -- --run apps/web-ele/src/modules/technical-workflow/views/DehydrationWorkstationView.test.ts`, `pnpm lint`, and `pnpm check:type` passed on 2026-06-09. |
| BUG-20260609-009 | Resolved | 脱水工作站表格行只显示状态 Tag 颜色，没有像标本确认页那样用整行底色区分可操作、进行中、已完成、异常或阻塞状态。 | 脱水人员在多行蜡块任务中不易快速扫描当前任务状态，跨工作站状态视觉不一致。 | 无需后端规避；前端在脱水页本地使用与标本确认页一致的 token 色语义添加整行背景和左侧状态条，不跨模块引用标本流程样式。 | Fixed in `apps/web-ele/src/modules/technical-workflow/views/DehydrationWorkstationView.vue`; covered by `DehydrationWorkstationView.test.ts`; `pnpm test:unit -- --run apps/web-ele/src/modules/technical-workflow/views/DehydrationWorkstationView.test.ts`, `pnpm lint`, and `pnpm check:type` passed on 2026-06-09. |
| BUG-20260609-010 | Resolved | 包埋工作站在待包埋列表行内填写“备注”后点击“确认包埋”或“确认包埋完成”，右侧已包埋蜡块列表的“包埋备注”仍显示 `-`。后端核对：`../SYBaseProject/bl-center/src/main/java/com/company/bl/interfaces/controller/EmbeddingController.java` 将完成包埋请求 `remarks` 写入包埋记录，查询汇总以 `embeddingRemarks` 返回。 | 包埋人员填写的蜡块备注在完成后不可见，影响后续切片和质控查看。 | 无需后端规避；前端完成包埋 payload 优先使用待包埋任务行 `remarks`，行备注为空时回退使用操作备注。 | Fixed in `apps/web-ele/src/modules/technical-workflow/views/EmbeddingWorkstationView.vue`; covered by `EmbeddingWorkstationView.test.ts`; `pnpm test:unit -- apps/web-ele/src/modules/technical-workflow/views/EmbeddingWorkstationView.test.ts`, `pnpm lint`, and `pnpm check:type` passed on 2026-06-09. |
| BUG-20260609-011 | Resolved | 包埋工作站顶部“确认包埋”点击后直接调用完成包埋逻辑，任务进入最终完成状态并提前创建切片待办；用户期望只有“确认包埋完成”才是最终包埋完成。后端核对：本次在 `../SYBaseProject/bl-center` 新增持久状态 `EMBEDDING_CONFIRM_PENDING` 并调整 `/api/v1/embeddings/start` / `/api/v1/embeddings/complete`。 | 包埋确认与完成两个业务动作被合并，导致切片工作站可能过早收到任务，包埋人员无法区分“已确认待完成”和“真正完成”。 | 前端“确认包埋”只调用 `startEmbedding` 并显示“包埋确认待完成”；“确认包埋完成”才调用 `completeEmbedding`，仅对 `EMBEDDING_CONFIRM_PENDING` 或 legacy `IN_PROGRESS` 启用。 | Fixed in `apps/web-ele/src/modules/technical-workflow/views/EmbeddingWorkstationView.vue`, `constants.ts`, sibling backend `../SYBaseProject/bl-center`, and covered by `EmbeddingWorkstationView.test.ts`, backend `TechnicalWorkflowExecutionIntegrationTest` / `TechnicalWorkflowQueryEnhancementIntegrationTest`; frontend targeted tests/typecheck passed, backend targeted Maven suites passed on 2026-06-09. Full frontend `pnpm lint` is blocked by unrelated dirty `specimen-workflow-mock-transport.ts`. |
| BUG-20260609-012 | Resolved | 诊断平台工作站中，采图区导入或拍照产生的图片只显示在右侧“已采图像”列表，报告预览编辑的“光镜所见”底部没有同步显示图片，也无法在报告纸张内拖动调整图片位置；多张图片默认可能重叠，采图区缩略图也缺少删除入口。 | 诊断医生无法把当前采图直接排布到报告光镜所见区域，截图/拍照材料和报告编辑视图割裂；误采图后无法从当前病例采图区直接移除。 | 无需后端规避；前端将当前病例采图传入报告编辑器，在默认病理报告模板的“光镜所见”文本框下方渲染可拖动图片层，默认按容器横向排列并超出换行；采图区卡片右上角提供删除按钮，删除后回收本地 object URL 并同步移除报告图片区图片。 | Fixed in `apps/web-ele/src/modules/doctor-workflow/views/DiagnosisWorkbenchView.vue`, `DiagnosisWorkbenchReportEditor.vue`, `DiagnosisWorkbenchTabs.vue`, shared `WorkbenchCapturedImagePanel.vue`, and `doctor-workflow.ts`; covered by `DiagnosisWorkbenchView.test.ts`; targeted Vitest, touched-file oxfmt/ESLint, `pnpm check:type`, and `pnpm lint` passed on 2026-06-09. Browser smoke reached the route but unauthenticated access redirected to `/auth/login`. |
| BUG-20260609-013 | Resolved | 取材描写工作站点击“取材完成”时，后端日志 traceId `e2d45c5742fe4f2fb722ea7526038c6c` 返回 `409 RESOURCE_CONFLICT` / `Embedding box number already exists`；截图中当前病例提交盒号为 `A1/B1/C1`。业务确认：包埋盒号只需在同一病理号/病例内唯一，不需要全局唯一。 | 不同病理号复用常见盒号 `A1/B1/C1` 时被全局唯一校验误拦截，导致取材完成不能提交。 | 前端取材工作台改为生成并提交短盒号，初始化时根据当前病例 tracking 中已有 `sampling_blocks.embedding_box_no` / `embedding_boxes.embedding_box_no` 续号，避免同病例内重复；后端 sibling `../SYBaseProject/bl-center` 将查重和数据库唯一约束改为 `case_id + embedding_box_no`。 | Fixed in `apps/web-ele/src/modules/technical-workflow/composables/useGrossingWorkbench.ts` and sibling backend `../SYBaseProject/bl-center`; covered by `useGrossingWorkbench.test.ts`, `GrossingWorkstationView.test.ts`, and backend `TechnicalWorkflowExecutionIntegrationTest`. `pnpm test:unit -- --run apps/web-ele/src/modules/technical-workflow/composables/useGrossingWorkbench.test.ts apps/web-ele/src/modules/technical-workflow/views/GrossingWorkstationView.test.ts`, `pnpm lint`, `pnpm check:type`, and `.\mvnw.cmd -pl bl-center clean -Dtest=TechnicalWorkflowExecutionIntegrationTest test` passed on 2026-06-09. |
| BUG-20260609-015 | Resolved | 切片工作站「玻片打印」列表中“蜡块号”“包埋备注”显示 `-`。后端核对：`../SYBaseProject/bl-center/src/main/java/com/company/bl/infrastructure/persistence/JdbcTechnicalWorkflowProcessingQueries.java` 原 workbench 查询只在 `technical_pending_tasks.object_type='EMBEDDING_BOX'` 时 join `embedding_boxes`，旧形态切片任务若仍指向 `SAMPLING_BLOCK` 就取不到 `embeddingBoxNo` / `embeddingRemarks`。 | 切片人员无法确认待打印玻片对应蜡块号和包埋备注，虽然正式包埋盒号在数据库中非空。 | 后端 workbench 查询兼容 `SAMPLING_BLOCK` 任务，通过 `sampling_block_id` 找回真实包埋盒；`embeddingRemarks` 优先最终包埋记录备注，空时回退取材块 `embedding_remarks`。前端不使用 `embeddingBoxId` 伪装蜡块号，只消费后端真实 `embeddingBoxNo`。 | Fixed in sibling backend `../SYBaseProject/bl-center` and covered by `SlicingWorkbenchIntegrationTest` legacy-task case; frontend `SlicingWorkstationView.test.ts` and `technical-workflow-service.test.ts` now assert actual print-row values. Frontend targeted Vitest and `pnpm check:type` passed; touched-file oxfmt/ESLint passed. |
| BUG-20260609-014 | Resolved | 诊断平台工作站三栏之间虽然存在分隔按钮，但分栏使用比例下限和面板最小宽度约束，用户拖动时版块无法按截图预期自由调节大小。 | 诊断队列、报告预览编辑、诊断材料区在不同桌面宽度下不便按当前工作重点调整显示面积。 | 无需后端规避；前端工作台改为可见 10px 分隔条，使用带像素下限的 `minmax` 栅格轨道，左侧分隔条调整队列/报告，右侧分隔条调整报告/材料区，并支持键盘方向键微调。 | Fixed in `apps/web-ele/src/modules/doctor-workflow/views/DiagnosisWorkbenchView.vue` and covered by `apps/web-ele/src/modules/doctor-workflow/views/DiagnosisWorkbenchView.test.ts`; `pnpm test:unit -- --run apps/web-ele/src/modules/doctor-workflow/views/DiagnosisWorkbenchView.test.ts`, touched-file oxfmt/ESLint, `pnpm check:type`, and `pnpm lint` passed on 2026-06-09. |
| BUG-20260609-016 | Resolved | 离体确认页按标本编号快捷确认未绑定条码标本时，前端先通过 `GET /api/v1/specimens?keyword=<标本编号>` 命中记录，再调用 sibling backend `POST /api/v1/specimen-removals/confirm-by-identifier`；后端日志 traceId `ea204176904b4f469f6e5ec3550deaec` 显示 `quick_confirm_specimen_removal` 二次调用 `findSpecimenByBarcode(null)`，在 `Map.of("barcode", null)` 触发 `NullPointerException` 并返回 500。 | 用户无法通过标本编号完成未绑定条码标本的离体确认，页面显示查询/确认失败。 | 前端契约保持不变；后端 sibling `../SYBaseProject/bl-center` 在按标识解析到标本后直接使用该标本完成离体确认，不再二次按条码查询。 | Fixed in sibling backend `../SYBaseProject/bl-center/src/main/java/com/company/bl/application/service/SpecimenReceiptAndRemovalService.java`; covered by `SpecimenReceiptAndRemovalServiceTest#quickRemovalConfirmationShouldSupportSpecimenNumberBeforeBarcodeBinding`. Backend `.\mvnw.cmd -pl bl-center -Dtest=SpecimenReceiptAndRemovalServiceTest clean test` passed; frontend `pnpm exec vitest run --dom --run apps/web-ele/src/modules/specimen-workflow/views/FixationVerifyView.test.ts apps/web-ele/src/modules/specimen-workflow/api/specimen-workflow-service.test.ts` passed on 2026-06-09. |
| BUG-20260609-017 | Resolved | 点击“切片工作站”后页面请求 sibling backend `GET /api/v1/slicings/workbench` 返回 500；后端日志 traceId `bf7293b74dfa462d9c987cb4a1f117db` 显示达梦报 `不是 GROUP BY 表达式`，根因是 `JdbcTechnicalWorkflowProcessingQueries.findSlicingWorkbenchRows` 的 grouped query 在 select 表达式中使用 `t.object_type`，但未将其加入 `GROUP BY`。 | 切片工作站打开失败，待打印、待切片和今日已完成列表无法加载；前端菜单、路由和页面接口调用本身无变更需求。 | 前端契约保持不变；后端 sibling `../SYBaseProject/bl-center` 在切片 workbench grouped query 中补齐 `t.object_type`，保留 `EMBEDDING_BOX` 与 legacy `SAMPLING_BLOCK` 解析逻辑。 | Fixed in sibling backend `../SYBaseProject/bl-center/src/main/java/com/company/bl/infrastructure/persistence/JdbcTechnicalWorkflowProcessingQueries.java`; backend `.\mvnw.cmd -pl bl-center -DskipTests compile` and `.\mvnw.cmd -pl bl-center "-Dtest=SlicingWorkbenchIntegrationTest" test` passed on 2026-06-09. |
| BUG-20260609-018 | Resolved | 切片工作站 `玻片打印` tab 点击 `打印玻片` 时只能单选 1 行，前端不弹浏览器打印窗口，接口成功后自动切换到 `切片` tab。 | 切片人员不能一次勾选多条批量打印玻片标签，打印后还会被自动带离打印列表，影响继续核对或补打剩余玻片。 | 无需后端规避；前端在点击时同步打开打印窗口，逐条提交选中行的 `POST /api/v1/slicings/slide-print`，把返回玻片号写入同一打印文档，刷新数据但保持 `玻片打印` tab。 | Fixed in `apps/web-ele/src/modules/technical-workflow/views/SlicingWorkstationView.vue`; covered by `SlicingWorkstationView.test.ts` batch print case. `pnpm test:unit -- apps/web-ele/src/modules/technical-workflow/views/SlicingWorkstationView.test.ts`, touched-file `oxfmt --check`, and `git diff --check` passed on 2026-06-09. Full lint/typecheck are blocked by unrelated dirty-worktree items recorded in `TD-20260609-002` and `TD-20260609-003`. |
| BUG-20260609-019 | Resolved | 标本出库页输入/扫描标本条码或标本编号后，前端向 sibling backend `GET /api/v1/specimen-outbounds?identifier=<barcode-or-specimenNo>` 查询；真实后端只按该标本精确过滤，截图中申请单 `1127` 只回显 `SP20260609006` 一条，未展示该申请单下其他标本。Mock 侧已是唯一命中后按申请单展开。 | 出库人员扫描其中一个标本时无法核对同一申请单下的所有标本，容易遗漏同单其他待出库标本。 | 前端 API 契约和 UI 不变；后端 sibling `../SYBaseProject/bl-center` 将唯一命中的 `identifier` 解析为该标本所属 `applicationId` 查询，前端测试锁定无路由上下文扫码后展示同申请单多条标本且只把精确命中行标为“出库未保存”。 | Fixed in sibling backend `../SYBaseProject/bl-center/src/main/java/com/company/bl/application/service/SpecimenWorkflowRemovalQuerySupport.java`; backend recorded as `BUG-20260609-009`. Frontend covered by `TransportHandoverView.test.ts`; `pnpm test:unit -- --run apps/web-ele/src/modules/specimen-workflow/views/TransportHandoverView.test.ts apps/web-ele/src/modules/specimen-workflow/api/specimen-workflow-service.test.ts`, `pnpm check:type`, and `pnpm lint` passed on 2026-06-09. Backend target suite passed earlier; later rerun is blocked before specimen assertions by unrelated dirty M3 slicing compile mismatches. |
| BUG-20260609-020 | Resolved | 标本固定、标本确认、标本入库、标本出库页面对未绑定条码标本存在本地“缺少条码”阻断或只提交空条码，导致已经有 `specimenId` 的行操作不能推进。 | 未绑定条码的标本无法完成固定、确认、入库或创建转运出库，和“条码绑定只是可选关联/扫码便利”的业务规则冲突。 | 前端行级操作改为发送 `specimenId`，保留 `specimenBarcode` / `specimenNo` 兼容；出库创建转运单发送 `specimenIds`，不因 `barcode=null` 禁用操作。 | Fixed across `apps/web-ele/src/modules/specimen-workflow`; sibling backend `../SYBaseProject/bl-center` resolves workflow requests by `specimenId > specimenBarcode > specimenNo`. Frontend targeted Vitest (6 files, 61 tests), `pnpm lint`, `pnpm check:type`, and backend targeted Maven suite (47 tests) passed on 2026-06-09. |
| BUG-20260609-021 | Resolved | 标本出库页扫描/输入标本后加入“出库未保存”，点击“转运”成功后，因为输入框已被清空且页面继续无条件刷新列表，表格变成 0 条，用户看不到刚转运的标本和同申请单其他标本。 | 出库人员转运成功后无法留在当前结果中核对转运状态，只能重新扫码/查询；截图显示成功后列表被隐藏清空。 | 无需后端规避；前端成功后保留当前 `items`，将已提交标本本地标记为 `IN_TRANSIT` / “已出库”且不可再选，清空选择和待出库草稿 ID；只有用户点击“清除列表”才清空表格。 | Fixed in `apps/web-ele/src/modules/specimen-workflow/views/TransportHandoverView.vue`; covered by `TransportHandoverView.test.ts` and `transport-handover.test.ts`. Targeted Vitest (2 files, 16 tests) and touched-file eslint/oxfmt/diff checks passed on 2026-06-09; full `pnpm lint` / `pnpm check:type` are currently blocked by unrelated dirty slicing workflow issues. |
| BUG-20260609-022 | Resolved | 标本接收工作站点击“标本签收”时，前端对未绑定条码标本仍提交空 `specimenBarcode`，sibling backend `POST /api/v1/specimen-receipts` 由 `ReceiveSpecimensRequest.ReceiptItem.specimenBarcode @NotBlank` 拦截，日志显示 2026-06-09 `traceId=0e0f1a97be5f487da5547bdb384eb21a` 返回 400。后续复核发现离体确认表格行按钮也在无条码时本地提示“请先录入或扫描标本条码”。 | 条码本身不是必填业务条件，但未绑定条码标本无法签收；若进入后续离体确认行按钮也会被条码本地校验挡住。 | 前端签收草稿和请求改为携带 `specimenId` / nullable `specimenBarcode` / `specimenNo`，校验任一标识存在即可；离体确认行按钮在无条码时走 `confirm-by-identifier` 的 `SPECIMEN_NO` 路径。Sibling backend 接收 DTO 和服务解析同样按 `specimenId > specimenBarcode > specimenNo`。 | Fixed in `apps/web-ele/src/modules/specimen-workflow/utils/specimen-receipt.ts`, `useSpecimenReceiptWorkbench.ts`, `FixationVerifyView.vue`, related tests, and sibling backend `../SYBaseProject/bl-center`; frontend targeted Vitest (5 files, 26 tests) and backend `SpecimenReceiptAndRemovalServiceTest` passed on 2026-06-09. Full frontend `pnpm lint` / `pnpm check:type` remain blocked by unrelated dirty `SlicingWorkstationView.vue` and dashboard issues. |
| BUG-20260609-023 | Resolved | 取材描写工作站左侧任务列表、中间描写工作区、右侧取材模板三列不能像诊断平台工作站一样自由拖拽调整大小；中间“已采图像”区域只能在组件内部滚动，无法跟随整个描写工作区一起滚动。 | 取材人员在不同屏幕宽度下无法按当前任务重点调整三列空间，已采图像较多时还会被局部滚动容器困住，查看描写内容、摄像头预览和图片列表不连贯。 | 无需后端规避；前端取材页面复用诊断平台的 split handle 思路，使用带像素下限的三列 `minmax` 栅格和两条 10px 分隔条；共享 `WorkbenchCapturedImagePanel` 新增默认不变的 `scrollMode`，取材页面显式使用 `external`。 | Fixed in `apps/web-ele/src/modules/technical-workflow/views/GrossingWorkstationView.vue` and `apps/web-ele/src/modules/shared/components/WorkbenchCapturedImagePanel.vue`; covered by `GrossingWorkstationView.test.ts`. `pnpm test:unit -- --run apps/web-ele/src/modules/technical-workflow/views/GrossingWorkstationView.test.ts`, touched-file oxlint/eslint, and `git diff --check` passed on 2026-06-09. Full typecheck is blocked by existing `TD-20260609-005`; authenticated browser smoke was not completed because the route redirected to login. |
| BUG-20260609-024 | Resolved | 脱水工作站勾选多条取材完成后生成的脱水任务时，只要混入 `IN_PROGRESS` 或 `COMPLETED` 行，原“开始脱水/脱水完成”批量逻辑会因要求全部同状态而整体拦截；`PENDING` 行点击“脱水完成”也不能自动先开始再完成。后端核对：sibling backend `../SYBaseProject/bl-center/src/main/java/com/company/bl/application/service/TechnicalGrossingWorkflowService.java` 的 `completeDehydration` 仍要求任务先处于 `IN_PROGRESS`。 | 脱水人员在同一病理号多蜡块列表中无法按截图所示混选后快速批量开始或完成，取材完成过来的待处理任务需要额外分步操作。 | 前端批量动作改为处理可操作项：开始只处理 `PENDING`；完成对 `PENDING` 先调用 `startDehydration` 再调用 `completeDehydration`，对 `IN_PROGRESS` 直接完成，`COMPLETED` 跳过并汇总提示。 | Fixed in `apps/web-ele/src/modules/technical-workflow/views/DehydrationWorkstationView.vue`; covered by `DehydrationWorkstationView.test.ts`. `pnpm test:unit -- --run apps/web-ele/src/modules/technical-workflow/views/DehydrationWorkstationView.test.ts` and touched-file oxfmt/ESLint/diff checks passed on 2026-06-09. Full `pnpm lint` / `pnpm check:type` remain blocked by unrelated dirty slicing/dashboard issues. |
| BUG-20260610-001 | Resolved | 包埋工作站点击“确认包埋”后，`EMBEDDING_CONFIRM_PENDING` / “包埋确认待完成”任务仍留在左侧待包埋列表，右侧“已包埋蜡块列表”为空或只展示最终完成记录；右侧待完成行的“切片备注”和“取材评价”显示为 `-` 且不可编辑；右侧表格横向滚动时“状态”列不能固定显示。后端核对：`../SYBaseProject/bl-center/src/main/java/com/company/bl/application/service/TechnicalWorkflowQueryService.java` 的 `getEmbeddingWorkstationSummary` 将 `EMBEDDING_CONFIRM_PENDING` 放在 `pendingTasks`，`completedRecords` 仅代表最终完成记录。 | 包埋人员无法在右侧列表继续执行“确认包埋完成”前补充切片备注和取材评价，且横向滚动时状态列不易持续核对。 | 无需后端规避；前端左侧普通待包埋查询只请求 `taskStatus=PENDING`，右侧将 summary 中 `EMBEDDING_CONFIRM_PENDING` / legacy `IN_PROGRESS` 的 pending task 映射为可完成行并与最终完成记录同表展示；待完成行切片备注和取材评价先写入行级草稿，点击“确认包埋完成”时随 `completeEmbedding` 提交；右侧“状态”列使用 sticky 右固定。 | Fixed in `apps/web-ele/src/modules/technical-workflow/views/EmbeddingWorkstationView.vue` and `EmbeddingQualityReviewDialog.vue`; covered by `EmbeddingWorkstationView.test.ts` and `EmbeddingQualityReviewDialog.test.ts`. `pnpm test:unit -- --run apps/web-ele/src/modules/technical-workflow/views/EmbeddingWorkstationView.test.ts apps/web-ele/src/modules/technical-workflow/components/EmbeddingQualityReviewDialog.test.ts` passed on 2026-06-10 (18 tests); touched-file `oxfmt --check`, `oxlint`, and `git diff --check` passed. Full `pnpm lint` is blocked by unrelated `SlicingWorkstationView.vue` style order issue; full `pnpm check:type` is blocked by unrelated `dashboard-specimen.ts` nullable title issue. Browser smoke reached the route but unauthenticated access redirected to login with slider verification. |
| BUG-20260610-002 | Resolved | 染色出片工作站右侧“已完成出片”直接由左侧选中待办的病例追踪结果派生；连续在待出片列表中选择不同任务并点击“染色出片”时，之前已完成的出片记录会被新选中病例的追踪数据替换。后端核对：`../SYBaseProject/bl-center/src/main/java/com/company/bl/interfaces/controller/SlideStainingController.java` 的完成接口仅返回 `taskId`、`slideId`、`caseStatus`，当前没有“今日已完成出片”查询接口。 | 染色人员无法在当前工作站页面连续核对本次已经完成的多张出片记录，容易误以为上一张记录丢失。 | 不新增后端接口；前端将右侧列表改成本次页面内累计完成记录，只有完成染色后才用现有病例追踪接口补齐该玻片展示信息并按 `slideId` 去重，切换左侧待办不再清空或替换右侧列表。 | Fixed in `apps/web-ele/src/modules/technical-workflow/views/StainingWorkstationView.vue` and `apps/web-ele/src/modules/technical-workflow/components/StainingProcessDialog.vue`; covered by `StainingWorkstationView.test.ts`. `pnpm test:unit -- --run apps/web-ele/src/modules/technical-workflow/views/StainingWorkstationView.test.ts`, `pnpm lint`, and `pnpm check:type` passed on 2026-06-10. Browser smoke reached `/technical-workflow/staining` but unauthenticated access redirected to login with slider verification, so authenticated visual verification was not completed. |
| BUG-20260611-001 | Resolved | 系统管理菜单下的“日志管理”在真实后端菜单模式下未显示图标；前端复核发现问题有两层：`apps/web-ele/src/api/core/menu-mapper.ts` 原先在 `SYS_LOG_MANAGEMENT` 菜单 icon 为空时不会回退到前端 canonical 路由图标；同时静态系统路由与 fallback 路由使用的 `carbon:document-audit` 在当前项目实际 Iconify 解析链路下会渲染为空白方框，占位但不出图标。 | 用户在左侧系统管理导航中看到“日志管理”前存在空白占位，菜单层级视觉不一致。 | 无需后端规避；前端将日志管理 canonical 图标统一改为项目内已使用且可渲染的 `carbon:report`，并在菜单映射层增加 icon alias 归一化：后端 icon 为空时回退到 canonical 图标，后端若仍返回旧值 `carbon:document-audit` 也自动映射到 `carbon:report`。 | Fixed in `apps/web-ele/src/api/core/menu-mapper.ts`, `apps/web-ele/src/api/core/menu-fallback-routes.ts`, and `apps/web-ele/src/router/routes/modules/system.ts`; covered by `apps/web-ele/src/api/core/menu.test.ts` (missing-icon fallback + legacy invalid alias normalization). `pnpm test:unit -- --run apps/web-ele/src/api/core/menu.test.ts`, `pnpm check:type`, targeted `pnpm exec oxfmt --check apps/web-ele/src/api/core/menu-mapper.ts apps/web-ele/src/api/core/menu.test.ts apps/web-ele/src/api/core/menu-fallback-routes.ts apps/web-ele/src/router/routes/modules/system.ts`, and targeted `pnpm exec eslint` for the same files passed on 2026-06-11. Full `pnpm lint` remains blocked by unrelated dirty-worktree `operation-support` formatting/lint issues. |
| BUG-20260612-001 | Resolved | Linear `SID-86` closure Red Team found static `apps/web-ele/src/router/routes/modules/m6.ts` pointed `/m6/quality-indicators` and `/m6/management-indicators` to real pages, but backend-driven menu mapping and fallback menu mapping in `apps/web-ele/src/api/core/menu-component-definitions.ts` / `apps/web-ele/src/api/core/menu-fallback-routes.ts` still used `/views/_core/fallback/MenuPlaceholderView`. | When the backend menu or fallback route source was used, M6 quality and management menu entries could render placeholders even though the static route implementation was complete. | Dynamic backend-menu definitions and static fallback routes now map quality/management M6 menu entries to the real statistics pages. | Fixed in `apps/web-ele/src/api/core/menu-component-definitions.ts`, `apps/web-ele/src/api/core/menu-fallback-routes.ts`, and covered by `apps/web-ele/src/api/core/menu.test.ts`. Red-Green: targeted `menu.test.ts` first failed on placeholder mapping, then passed after the fix. Final validation on 2026-06-12: targeted M6/menu Vitest 7 files / 29 tests, `pnpm check:type`, `pnpm lint`, `pnpm build:ele`, Playwright desktop/mobile smoke for four M6 routes, and backend `M6StatisticsIntegrationTest,M6AuthorizationIntegrationTest,M6ObservabilityIntegrationTest` all passed. |
| BUG-20260612-002 | Resolved | Clean SID-86 frontend worktree dev-server validation failed from `.logs/frontend.log` with Tailwind/Vite pre-transform errors resolving `tw-animate-css`, then `@iconify/tailwind4` and `@tailwindcss/typography`. `internal/tailwind-config/src/theme.css` imports/plugins those packages, but `internal/tailwind-config/package.json` did not declare them. | Fresh worktrees or package-isolated installs could fail `pnpm dev:ele` / Vite CSS transform even though another checkout might have transitive/root dependencies available. | `@vben/tailwind-config` now owns direct dependencies for every package imported by its exported theme CSS/plugin file. | Fixed in `internal/tailwind-config/package.json`, `pnpm-workspace.yaml`, and `pnpm-lock.yaml`. Verified on 2026-06-12 by clean worktree `pnpm install`, Vite local smoke at `http://127.0.0.1:5786`, `pnpm lint`, and `pnpm build:ele` resolving Tailwind CSS/plugin imports successfully. |
| BUG-20260612-003 | Resolved | User-reported statistics dashboard load failure: frontend requested `POST http://localhost:5777/api/v1/stat-dashboard/query`, and backend access log at `2026-06-12T13:49:05.777875+08:00` showed `uri=/api/v1/stat-dashboard/query`, `status=404`, `traceId=126a212ff088424d96d7d9c8081671b5`. Backend cross-check found `../SYBaseProject/bl-center/src/main/java/com/company/bl/interfaces/controller/M6ManagementController.java` exposes `POST /api/v1/stat-reports/query` but no dashboard aggregate endpoint. | `/m6/dashboard` could fail immediately with “统计仪表盘加载失败” against the real backend because the frontend used a non-existent endpoint and menu aliases still referenced it. | Frontend `queryStatDashboard` now composes three existing report queries (`category=QUALITY`, `OPERATION`, `WORKLOAD`) and derives dashboard cards; backend-menu component definitions no longer alias `/api/v1/stat-dashboard/query`. | Fixed in `apps/web-ele/src/modules/m6-statistics/api/m6-statistics-service.ts` and `apps/web-ele/src/api/core/menu-component-definitions.ts`; covered by `m6-statistics-service.test.ts` asserting three `/v1/stat-reports/query` calls and no `/v1/stat-dashboard/query`, plus `menu.test.ts`. Validation on 2026-06-12: targeted Vitest (3 files, 24 tests), `pnpm check:type`, and `pnpm lint` passed. Browser verification reached authenticated-app validation only partially because route access depends on local login/session; no source/runtime call site remains outside the regression test guard. |
| BUG-20260615-001 | Resolved | 已登录后点击或进入诊断平台工作站时，Vue Router 报 `Failed to fetch dynamically imported module: http://localhost:5777/src/modules/doctor-workflow/views/DiagnosisWorkbenchView.vue`；`.logs/frontend.log` 同期出现 Vite dependency optimizer reload 和多个路由动态导入失败记录。 | 诊断平台工作站页面打不开，导航停留在错误状态。 | 浏览器手动刷新有时可恢复，但 Vite dev server 再次优化重载时仍可能复现。 | Fixed in `apps/web-ele/src/router/routes/lazy-load.ts` and `apps/web-ele/src/router/routes/modules/doctor-workflow.ts`; covered by `apps/web-ele/src/router/routes/lazy-load.test.ts` and `apps/web-ele/src/router/routes/modules/doctor-workflow.test.ts`. Validation on 2026-06-15: targeted Vitest, web-ele `vue-tsc --noEmit --skipLibCheck`, targeted `oxlint`, targeted `oxfmt --check`, and `git diff --check` passed. |
| BUG-20260615-002 | Resolved | 病理医嘱执行页截图反馈：页面仍展示上方“执行操作”卡片，医嘱列表含“医嘱号”列，且类型 `ROUTINE`、状态 `IN_PROGRESS`、收费状态 `SUCCESS` 原样显示英文。 | 医嘱执行人员在列表中看到英文枚举和不需要的列/模块，影响中文界面一致性与信息密度。 | 无需后端规避；前端展示层移除上方执行操作卡片和医嘱号列，并补齐医嘱类型、状态、收费状态中文映射。 | Fixed in `apps/web-ele/src/modules/doctor-workflow/views/MedicalOrderWorkbenchView.vue` and `apps/web-ele/src/modules/doctor-workflow/utils/format.ts`; covered by `apps/web-ele/src/modules/doctor-workflow/views/doctor-workflow-visibility.test.ts`. Validation on 2026-06-15: targeted Vitest and `pnpm check:type` passed; touched-file ESLint/oxlint/oxfmt/diff checks passed. Full `pnpm lint` is blocked by unrelated existing `scripts/validate-pr-packet.mjs` `unicorn/prefer-includes`. |
| BUG-20260615-003 | Resolved | 归档管理 `/operation-support/archive` 的申请单、蜡块、玻片、标本归档列表中未归档状态显示为空；申请单归档列表/弹窗缺少申请医生、申请时间；借阅状态 `NONE` 原样显示英文；归档列表高度可能超出屏幕。后端核对：sibling backend `../SYBaseProject/bl-center` 的 `GET /api/v1/archive-objects` 原 `ArchiveRecordView` 未返回申请医生/申请日期，且未归档对象 `archiveStatus` 可为空、`loanStatus=NONE` 为合法契约。 | 归档人员无法确认申请单来源信息和未归档/未借阅状态，页面信息密度和可视高度不符合归档工作台使用预期。 | 前端展示层将空归档状态/`NOT_ARCHIVED` 显示为“未归档”，`NONE` 显示为“未借阅”，列表容器限制在视口内；后端返回 `applicantDoctorName` / `applicationDate`，来源为 `applications.submitting_doctor_name` / `applications.application_date` 并以申请单字段名输出给前端。Backend recorded as `../SYBaseProject/KNOWN_BUGS.md#BUG-20260615-001`. | Fixed in `apps/web-ele/src/modules/operation-support` and sibling backend `../SYBaseProject/bl-center`; covered by frontend `format.test.ts`, `operation-support-service.test.ts`, `ArchiveManagementView.test.ts`, and backend `ArchiveWorkflowIntegrationTest`. Validation on 2026-06-15: frontend targeted Vitest (3 files, 18 tests), targeted `oxfmt --check`, `pnpm check:type`, backend `cmd /c mvnw.cmd -pl bl-center -Dtest=ArchiveWorkflowIntegrationTest test` passed. Browser opened `/operation-support/archive` on `http://localhost:5778` but unauthenticated access redirected to `/auth/login` with slider verification, so authenticated visual check was not completed. |  | BUG-20260616-001 | Resolved | 用户在 2026-06-16 点击 `/m6/quality-indicators` 的“查看明细”时，后端访问日志显示 `POST /api/v1/stat-report-details/query` 返回 `404`（示例时间 `2026-06-16T09:29:41+08:00`，`traceId=8949b4cb615b4c5ea4d25ca662069fa0`）。跨仓复核发现 sibling backend `../SYBaseProject/bl-center/src/main/java/com/company/bl/interfaces/controller/M6ManagementController.java` 仅暴露 `POST /api/v1/stat-reports/query` 与 `/export`，并不存在 `/api/v1/stat-report-details/*`。 | 质控指标统计页点击“查看明细”直接报错，用户无法完成后续分析，且前端 memory/测试会持续把不存在的接口当成既有契约。 | 前端移除对不存在明细接口的调用，改为在明细抽屉中展示“当前后端仅提供质控汇总查询，尚未开放独立明细查询/导出接口”的可用性说明，并导出同样的说明 CSV，避免再次触发 404；同时修正 memory 中错误的 M6 明细契约描述。 | Fixed in `apps/web-ele/src/modules/m6-statistics/api/m6-statistics-service.ts`, `apps/web-ele/src/modules/m6-statistics/views/QualityIndicatorStatisticsView.vue`, related M6 tests, and frontend memory files. Validation on 2026-06-16: targeted `pnpm test:unit -- --run apps/web-ele/src/modules/m6-statistics/api/m6-statistics-service.test.ts apps/web-ele/src/modules/m6-statistics/views/QualityIndicatorStatisticsView.test.ts`, targeted `pnpm exec eslint ...`, and `git diff --check` passed. `pnpm check:type` is still blocked by unrelated existing `operation-support` dirty-worktree/type errors (`operation-support-service.test.ts`, missing `EquipmentLedgerView.vue`). |  | BUG-20260617-001 | Resolved | 用户反馈“申请单列表已显示病理号，但诊断分配页按同一病理号查不到任务”；前端复核确认 `apps/web-ele/src/modules/doctor-workflow/views/DiagnosisAssignmentView.vue` 仍正常把 `pathologyNo` 传给 `GET /api/v1/diagnostic-tasks/pending`，而申请单列表显示的是 sibling backend `GET /api/v1/applications` 返回的病例病理号。跨仓排查发现 sibling backend `../SYBaseProject/bl-center` 诊断任务查询按 `diagnostic_tasks.pathology_no` 过滤，但技术登记改号链路只更新了 `pathology_cases.pathology_no`，未同步 `diagnostic_tasks`。 | 诊断分配页对最新病理号查询会漏掉真实存在的待分配任务，用户容易误判数据丢失；历史人工改号病例也会持续受影响。 | 本轮不改前端请求或页面逻辑；sibling backend 已将 `pathology_cases.pathology_no` 作为主数据，并在病理号更新时同步同病例全部 `diagnostic_tasks.pathology_no`，同时用 `V101__sync_diagnostic_task_pathology_no_from_cases.sql` 回填历史漂移数据。Backend records the storage/query root cause as `../SYBaseProject/KNOWN_BUGS.md#BUG-20260617-001`. | Resolved by sibling backend `../SYBaseProject/bl-center` only. Backend targeted Maven tests `DiagnosticWorkflowAssignmentPathologyNoQueryIntegrationTest#shouldListPendingDiagnosticTaskByUpdatedPathologyNoAfterCaseNumberChanges`, `TechnicalSpecimenRegistrationIntegrationTest#shouldSyncDiagnosticTaskPathologyNoWhenManualCandidateUpdatesCurrentCase`, and `DiagnosticTaskPathologyNoSyncV101MigrationTest` passed on 2026-06-17; no frontend code change or frontend validation command was required for this fix. |  | BUG-20260617-002 | Resolved | 用户在 2026-06-17 反馈诊断平台工作站查询病理号 `BL202605240001` 后点击“初步/复核”报错；提供的 backend 日志显示 `POST /api/v1/pathology-reports/RPT-c0b81351-963e-48e9-981f-103c8153a991/save-draft` 返回 `409 OPERATION_NOT_ALLOWED: Report is not editable draft`。前后端对照发现 `apps/web-ele/src/modules/doctor-workflow/views/DiagnosisWorkbenchView.vue` 顶部“暂存/初步/复核/签发”按钮此前只按权限启用，不按当前 `currentReport.reportStatus` 收口；而“初步”动作固定先调用 `save-draft` 再调用 `submit`。当当前报告已是 `SUBMITTED/REVIEWED/SIGNED/PUBLISHED` 时，前端仍允许点击，随后命中 sibling backend `../SYBaseProject/bl-center/src/main/java/com/company/bl/application/service/DiagnosticReportSupport.java#ensureDraftReport` 的草稿态校验。 | 诊断平台工作站会把非草稿报告继续当作可编辑草稿处理，导致用户在已提交/已复核/已签发报告上误触“初步/复核/签发”时收到 409 业务错误，误以为后台数据异常。 | 前端按 `currentReport.reportStatus` 增加按钮状态收口：仅 `DRAFT` 允许“暂存/初步”，仅 `SUBMITTED` 允许“复核”，仅 `REVIEWED` 允许“签发”；动作函数同时保留状态兜底，避免再次请求不匹配的接口。 | Fixed in `apps/web-ele/src/modules/doctor-workflow/views/DiagnosisWorkbenchView.vue` and covered by `apps/web-ele/src/modules/doctor-workflow/views/DiagnosisWorkbenchView.test.ts`. Validation on 2026-06-17: `pnpm test:unit -- --run apps/web-ele/src/modules/doctor-workflow/views/DiagnosisWorkbenchView.test.ts` passed; `pnpm run check:governance` passed. |  | BUG-20260617-003 | Resolved | 标本接收页输入标本号并回车后，前端正常调用 `GET /api/v1/specimens?keyword=<specimenNo>&page=1&size=500`，但 sibling backend `../SYBaseProject/bl-center` 标本管理 SQL 引用了不存在的 `application_registration_workbench_extensions` 表；当前 `V58__create_application_registration_workbench.sql` 已将 `inpatient_no` / `ward_name` 放在 `application_registration_workbench` 主表。 | 标本接收扫码查询返回“服务器内部错误”，无法通过标本号加载匹配标本用于后续签收。 | 本轮不改前端请求链路；后端改为从 `application_registration_workbench.inpatient_no` / `ward_name` 取数，并保持 `/api/v1/specimens` 响应中的 `inpatientNo` / `wardName` 字段。Backend records the SQL root cause as `../SYBaseProject/KNOWN_BUGS.md#BUG-20260617-002`. | Resolved by sibling backend `../SYBaseProject/bl-center` only. Backend targeted Maven command `cmd /c mvnw.cmd -pl bl-center -Dtest=ApplicationRegistrationWorkbenchIntegrationTest#shouldLookupByApplicationNoAndSaveWorkbenchForDownstreamFlow test` passed on 2026-06-17 with `MAVEN_OPTS=-Xmx2g`; no frontend code change or frontend validation command was required. |  | BUG-20260617-004 | Resolved | 用户点击“取材描写工作站”后页面打不开，浏览器控制台同时出现 `Failed to fetch dynamically imported module: http://localhost:5777/src/modules/technical-workflow/views/GrossingWorkstationView.vue?...`、`Navigation failed` 与 `504 (Outdated Optimize Dep)`；同类问题已在 2026-06-15 发生于诊断平台工作站。前端复核发现 `apps/web-ele/src/router/routes/modules/technical-workflow.ts` 仍直接懒加载各技术流程页面，而 `apps/web-ele/src/router/routes/modules/doctor-workflow.ts` 已对诊断平台工作站接入 `withRouteComponentReloadRetry`。 | Vite 开发态依赖重优化后，技术流程页面首次导航可能命中失效 chunk URL，导致工作站无法打开并停留在路由错误状态。 | 手动整页刷新通常可临时恢复；本轮前端把技术流程路由统一接入一次性重载重试保护，避免用户每次都手动刷新。 | Fixed in `apps/web-ele/src/router/routes/modules/technical-workflow.ts`; covered by `apps/web-ele/src/router/routes/modules/technical-workflow.test.ts`. Validation on 2026-06-17: targeted Vitest passed; browser verification confirmed `/technical-workflow/grossing` no longer crashes the app boot path and instead正常进入登录守卫或页面加载流程. |  | BUG-20260617-005 | Resolved | 用户反馈诊断分配页右侧“诊断任务列表”中 `病人ID / 检查项目 / 送检类型 / 蜡块数 / 送检科室 / 送检标本` 大量显示为 `-`。前端复核发现 `[DiagnosisAssignmentView.vue](/D:/Github/JW/SYBaseProjectWeb/apps/web-ele/src/modules/doctor-workflow/views/DiagnosisAssignmentView.vue)` 已读取这些字段，但 sibling backend `GET /api/v1/diagnostic-tasks/pending` 当时的 `PendingDiagnosticTaskResponse` / `TaskView` / `DiagnosticTask` 只返回任务基础字段，未包含病例摘要。 | 分派人员无法在分片页直接核对患者与送检摘要，容易误判数据未同步或病例信息丢失。 | 本轮不做前端逐行补查；跨仓扩展 pending 诊断任务合同，在 sibling backend 查询中补 `applications.patient_id / application_type / submitting_department_name`、最新 `application_registration_workbench.check_item`、病例级 `specimenName` 聚合和 `blockCount` 聚合，前端仅补类型归一化与回归测试。 | Fixed in frontend `apps/web-ele/src/modules/doctor-workflow/api/doctor-workflow-service.ts`, `doctor-workflow-service.test.ts`, `doctor-workflow-visibility.test.ts`, and sibling backend `../SYBaseProject/bl-center` diagnostic task query/VO/model chain. Validation on 2026-06-17: frontend targeted Vitest for doctor-workflow service + visibility tests passed; frontend `pnpm check:type` remained blocked by unrelated existing `technical-workflow` `patientIdDisplay` type errors; sibling backend `mvnw -pl bl-center compile` passed, while targeted backend test execution was blocked before running by unrelated existing `TechnicalTaskTimeoutPolicyTest` test-compile mismatch in the dirty sibling worktree. |  | BUG-20260617-006 | Resolved | 用户反馈检查号 `BL202606010001` 在取材描写工作站点击“取材”后出现顶部“服务器内部错误”。前端复核确认 `apps/web-ele/src/modules/technical-workflow/views/GrossingWorkstationView.vue` 的按钮链路仍是既有正确顺序：先 `POST /api/v1/grossings/start`，再刷新 `GET /api/v1/technical-tasks/pending`，最后加载 `GET /api/v1/grossings/{taskId}/context`。跨仓排查发现 sibling backend `../SYBaseProject/bl-center` 在引入 `patientIdDisplay` 后，取材相关技术任务查询/映射/响应链路对 `application_registration_workbench` 缺失记录的兼容性不足，导致该链路在空工作台扩展数据上出现 500 风险。 | 取材人员点击“取材”无法进入工作中状态，页面直接报服务器内部错误；同类缺少 `application_registration_workbench` 记录的历史病例也会持续受影响。 | 本轮不改前端业务逻辑或请求契约；sibling backend 已将 `patientIdDisplay` 在技术任务查询、RowMapper、`PendingTechnicalTaskResponse` 和 `GrossingWorkbenchContextResponse` 链路中按可空字段兼容处理，并补回“缺少 `application_registration_workbench` 记录时仍可开始取材并加载上下文”的回归测试。Backend also simplified an unrelated diagnostic pending-task aggregation SQL that surfaced as a separate runtime 500 during the regression run. Backend records the root cause as `../SYBaseProject/KNOWN_BUGS.md#BUG-20260617-003`. | Resolved by sibling backend `../SYBaseProject/bl-center` only. Backend targeted Maven command `.\mvnw.cmd -pl bl-center "-Dtest=TechnicalWorkflowQueryEnhancementIntegrationTest,TechnicalWorkflowExecutionIntegrationTest" test` passed on 2026-06-17; the run covered `TechnicalWorkflowQueryEnhancementIntegrationTest#shouldStartGrossingAndLoadContextWithoutRegistrationWorkbenchRecord`, and access logs in the test output showed `POST /api/v1/grossings/start` plus the follow-up pending/context requests returning `200`. Frontend code change and frontend validation commands were not required for this fix. |
