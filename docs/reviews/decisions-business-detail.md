# Business Decisions Detail

Full contract text for active business decisions. Index: [docs/memory/DECISIONS.md](../memory/DECISIONS.md). Governance-process history: [decisions-archive.md](./decisions-archive.md).

| ID | Date | Topic | Decision | Impact |
| --- | --- | --- | --- | --- |
| DEC-20260608-004 | 2026-06-08 | M2 application registration workbench | When an `APPLICATION_NO` lookup misses, the frontend reuses the existing application create API, generates editable placeholder patient/workbench fields, saves them through the workbench patient-info… | Auto-create depends on `PERM_APPLICATION_CREATE`; non-application-number lookup types do not create records to avoid treating names, inpatient numbers, or rout… |
| DEC-20260608-005 | 2026-06-08 | Technical specimen registration pathology number editing | The frontend sends an optional completion-time `pathologyNo` candidate with `applicationType`; non-empty candidates are not auto-completed by the backend and must already match the selected type and … | Frontend previews are candidate values only; users see/type the draft beside “送检标本”, and backend 400/409 responses are the source of truth. |
| DEC-20260608-006 | 2026-06-08 | Slicing workstation print-before-slice flow | The slicing workstation treats backend-confirmed slide printing (`POST /api/v1/slicings/slide-print`) as the required transition before a task appears in the slicing tab; `completeSlicing` no longer … | Frontend uses `pendingPrintList` for `玻片打印`, `pendingSliceList` for `切片`, and sends `applicationType=ROUTINE/FROZEN` filters that affect backend statistics and… |
| DEC-20260608-007 | 2026-06-08 | Tracking and abnormal application list pathology number display | Use backend `GET /api/v1/applications` `items[].pathologyNo` as the single source for the “病理号” column; frontend does not fan out detail requests to fill the list. As of 2026-06-17, the same endpoint… | Backend and frontend list item/query contracts must stay aligned; empty values mean the application has not produced a pathology case yet and display as `-`. T… |
| DEC-20260608-008 | 2026-06-08 | Historical-status lookup in M2/M3 workflow pages | Ordinary page entry remains empty, while explicit business-key queries may return all-status historical rows; action availability is still decided by each row's current workflow status. For M2 outbou… | M2 specimen pages use existing status/reason display for old rows; M3 grossing/dehydration only pass backend `includeAllStatuses=true` for explicit query/deep-… |
| DEC-20260609-001 | 2026-06-09 | M2 specimen check-in and transport operator identity | The selected current login user may act as the check-in / outbound / handover operator without an `operatorVerificationToken`; selected non-current operators still require the existing operator verif… | Frontend payload builders must omit `operatorVerificationToken` only for the current user; backend check-in / transport controllers must fall back to current r… |
| DEC-20260609-002 | 2026-06-09 | M2 specimen barcode binding semantics | A specimen registered without an explicit barcode remains `UNBOUND` with `barcode=null`; `POST /api/v1/specimens/{specimenId}/barcode-binding` is the first-write path for `targetBarcode`. Frontend pa… | `SpecimenTrackingSummary.barcode` is nullable in frontend types; transport creation excludes unbound specimens; barcode binding table/export place “标本条码” immed… |
| DEC-20260609-003 | 2026-06-09 | M3 embedding confirmation workflow | `POST /api/v1/embeddings/start` is the confirmation action and must persist `EMBEDDING_CONFIRM_PENDING` / “包埋确认待完成”; `POST /api/v1/embeddings/cancel` may return only that middle state to `PENDING` / … | Frontend top “确认包埋” must not call `completeEmbedding`; right-side “取消包埋” is enabled only for `EMBEDDING_CONFIRM_PENDING`; completion buttons are enabled only f… |
| DEC-20260609-004 | 2026-06-09 | M3 slicing workstation print controls | `SlicingWorkstationView` no longer exposes the marked print-parameter controls for slide count, adjacent merge/cancel merge, printer code, print remarks, pending-today filtering, or print-strategy he… | Frontend `POST /api/v1/slicings/slide-print` calls from this page send `sourceSlideCount=1`, `mergeAdjacent=false`, and blank printer/remarks unless product re… |
| DEC-20260609-005 | 2026-06-09 | M3 slicing workstation slide print handoff | Clicking `打印玻片` opens a browser print window, supports multiple selected rows, writes generated slide labels into one print document, refreshes the workbench, and keeps the active tab on `玻片打印` after… | `SlicingWorkstationView` calls `POST /api/v1/slicings/slide-print` once per selected print row using the fixed defaults from `DEC-20260609-004`; frontend must … |
| DEC-20260609-006 | 2026-06-09 | M2 specimen workflow identifiers | 标本固定、标本确认、标本入库、标本签收、标本出库推进动作以 `specimenId` 为行级首选标识，保留 `specimenBarcode` / `specimenNo` 兼容扫码和旧调用；条码绑定仅是关联与扫码便利，不是这些流程的前置条件。 | Frontend workflow payloads send `specimenId` when the user acts on a row; receipt payloads send `specimenId > specimenBarcode > specimenNo`; transport creation… |
| DEC-20260609-007 | 2026-06-09 | M3 slicing pending slide-print merge groups | Pending slide-print merge is persisted server-side and scoped to same patient + same case/pathology number + same embedding-box letter prefix; the frontend only requests grouping/cancel/print and ren… | Frontend uses `POST /api/v1/slicings/slide-print-merge-groups`, `/cancel`, and `/print`; merged workbench rows use `printGroupId`, `mergedPrintGroup`, `taskIds… |
| DEC-20260610-002 | 2026-06-10 | M5 archive and borrow page organization | Keep `归档管理` and `借记管理` as separate menu pages and organize each page internally with tabs; the archive page owns application-form/蜡块/slide/cabinet/record work, while the borrow page owns slide/蜡块 bor… | Future M5 frontend work should not re-merge borrow controls into the archive page or add unsupported old-system capabilities without a new backend/API/DB/exter… |
| DEC-20260610-003 | 2026-06-10 | 全站页面级标题头样式 | `@vben/common-ui` `Page` 新增显式 `showHeader` 开关，`apps/web-ele` 现有页面统一传 `show-header=false`，移除页面级大标题/说明头，仅保留正文、卡片内标题、面包屑、标签页与业务工具区。 | 后续新页面默认应沿用无页头模式；只有明确需要页面级头部时才显式保留或开启 `showHeader`。 |
| DEC-20260610-004 | 2026-06-10 | 系统日志管理与操作审计 | 系统管理只新增单菜单 `/system/logs`，页面内用 登录日志 / 操作日志 两个 tab；前端查询入口统一走 sibling backend `../SYBaseProject/bl-center` 的 `/api/v1/system/logs/*`。操作审计定义为所有已认证、带权限注解的写操作，加上显式 `@AuditOperation(sensitiveQuery = true)`… | 前端路由 authority 使用 `PERM_SYS_LOG_QUERY`，详情请求依赖后端 `PERM_SYS_LOG_DETAIL`；日志页本身属于敏感查询，会被审计但不得递归写日志；列表不展示长操作内容，详情展示后端脱敏截断内容。 |
| DEC-20260611-002 | 2026-06-11 | M5 archive cabinet maintenance | `归档柜列表` is a real full-stack tree-table workflow, not a screenshot-only placeholder: frontend calls sibling backend `POST /api/v1/archive-cabinets/batch` for prefix/serial batch creation and `DELETE … | Frontend route authorities include `PERM_M5_ARCHIVE_CABINET_DELETE`; batch create reuses create permission; no `SPECIMEN` / 标本柜 type is introduced. Backend `V8… |
| DEC-20260611-003 | 2026-06-11 | M5 reagent inventory/template contract | `/operation-resources/reagents` is organized as two tabs: `试剂库存` for stock batches and `试剂模板` for reusable reagent definitions. Sibling backend `../SYBaseProject/bl-center` owns the durable split whe… | Frontend service/types/dialogs/tests must keep the template-vs-stock distinction, consume stock action/event endpoints, and treat CSV row errors as backend-ret… |
| DEC-20260611-004 | 2026-06-11 | M5 archive object list contract | `/operation-support/archive` object tabs must use sibling backend `GET /api/v1/archive-objects` for paged object全集 by `APPLICATION_FORM`, `EMBEDDING_BOX`, or `SLIDE`; `GET /api/v1/archive-records/sea… | Each archive object tab owns independent `keyword/page/size/total/items/loading/error` state and refreshes the current tab after archive success. The new endpo… |
| DEC-20260611-005 | 2026-06-11 | AI automatic commit granularity | AI commits use acceptance-point grouping plus low/medium/high risk ratings. AI must explain proposed commit groups before committing; low and medium risk groups may be auto-committed after that expla… | Future AI commit flows must list included/excluded files, verification evidence, risk rating, risk rationale, and rollback impact before staging. Path-limited … |
| DEC-20260611-007 | 2026-06-11 | M5 specimen archive contract | `SPECIMEN` is now a supported M5 archive object type with independent permission `PERM_M5_SPECIMEN_ARCHIVE`, frontend tab `标本归档`, backend `POST /api/v1/archive/specimens`, and object pagination throu… | `/operation-support/archive` can show and archive specimens; archive dialog/buttons must check specimen permission; borrow pages and material-loan APIs must no… |
| DEC-20260611-008 | 2026-06-11 | M6 statistics report contract | Frontend statistic report queries use `workloadUserId` for workload/operator filtering and must not send legacy `operatorUserId` / `operatorName`. Query responses may include optional metadata (`metr… | `apps/web-ele/src/modules/m6-statistics` should tolerate old sparse rows and display/use extended metadata when pages add richer cards or drilldowns. Sibling b… |
| DEC-20260611-009 | 2026-06-11 | M6 statistics dashboard contract | `/m6/dashboard` is the default M6 statistics landing page. It reuses the existing statistic report query permission scope instead of adding a new permission model; backend menu seed uses `M6_SUPPORT`… | Frontend `M6_ROUTE_ITEMS`, static route, fallback menu route, and backend-menu mapper should point statistics users to `/m6/dashboard`; service/page code must … |
| DEC-20260611-010 | 2026-06-11 | M6 quality indicator overview and export | `/m6/quality-indicators` is a real quality-statistics overview page, not a placeholder. It queries `POST /api/v1/stat-reports/query` with `category=QUALITY`, surfaces `AVAILABLE` / `PARTIAL` / `UNAVA… | Supersedes the export-column portion of `DEC-20260611-008`; sparse query rows remain compatible, but report CSV consumers must now tolerate the extended column… |
| DEC-20260611-012 | 2026-06-11 | M6 management indicator statistics | `/m6/management-indicators` is a real management-statistics page that composes two report queries: `category=OPERATION` for business volume/billing/material warning metrics and `category=WORKLOAD` fo… | Frontend must keep management statistics under `apps/web-ele/src/modules/m6-statistics`, reuse `queryStatReport` / `exportStatReport`, and avoid legacy `operat… |
| DEC-20260613-004 | 2026-06-13 | Pathology receipt route ownership | `PathologyReceipt` is owned by the M2 specimen workflow route module at `/workflow/pathology-receipt`; M3 technical workflow keeps only entry/navigation links and root authority visibility for users … | Future M3 work should link to `/workflow/pathology-receipt` instead of registering a duplicate page route. Route/menu tests should preserve unique route names … |
| DEC-20260615-004 | 2026-06-15 | M5 material loan status query | `/operation-support/borrow` uses a single borrow-record list filtered by `loanStatus=BORROWED` or `loanStatus=RETURNED`; sibling backend `../SYBaseProject/bl-center` exposes `GET /api/v1/material-loa… | Frontend loan filters default to `BORROWED`, refresh after return keeps the current status filter, returned rows show return metadata and no return action, and… |
| DEC-20260615-005 | 2026-06-15 | M5 material loan abnormal registration | `/operation-support/borrow` registers wax-block/slide abnormal-loan records through sibling backend `../SYBaseProject/bl-center` `POST /api/v1/material-loans/abnormal-records`; the endpoint persists … | Frontend borrow tabs query material全集 from `GET /api/v1/archive-objects`, expose `借记 / 归还 / 异常登记` next to query controls, map local borrow-page status text as … |
| DEC-20260615-006 | 2026-06-15 | M5 physical archive batch contract | `/operation-support/archive` treats 蜡块、玻片、标本 archive tabs as multi-select batch workflows. The frontend submits `archiveCabinetId + objectIds[]` to sibling backend batch endpoints and no longer asks … | Frontend uses `POST /api/v1/archive/embedding-boxes/batch`, `/slides/batch`, and `/specimens/batch`; specimen batch archive may send `archiveExpiresAt` and `ar… |
| DEC-20260615-007 | 2026-06-15 | M5 archive cabinet node contract | `/operation-support/archive` `归档柜列表` consumes sibling backend `../SYBaseProject/bl-center` `GET/POST/PATCH /api/v1/archive-cabinet-nodes` as the source for AREA / CABINET / DRAWER nodes. New cabinet … | Frontend create options expose only `申请单 / 蜡块 / 玻片 / 标本 / 试管`; legacy `STANDARD` remains display-compatible but is not offered for new nodes. Batch cabinet cre… |
| DEC-20260615-008 | 2026-06-15 | M5 archive-page material loan shortcut | `/operation-support/archive` may expose a selected-row `借记` shortcut for `EMBEDDING_BOX` and `SLIDE` only, reusing the existing material-loan dialog and `POST /api/v1/material-loans` one-by-one submi… | This supersedes only the “no borrow controls on archive page” part of `DEC-20260610-002`: archive-page shortcuts must keep permission gating with `PERM_M5_LOAN… |
| DEC-20260615-009 | 2026-06-15 | M4 pathology report list workflow page | `/doctor-workflow/report` is now a case-scoped report list workflow page rather than a draft editor. Users query by `caseId` or `pathologyNo`, the page lists current-case report lifecycle rows throug… | Frontend no longer shows `任务ID` / `报告正文` / `创建草稿` / `保存草稿` / `提交` on this page. The report list covers `DRAFT / SUBMITTED / REVIEWED / SIGNED / PUBLISHED`; dra… |
| DEC-20260615-010 | 2026-06-15 | M4 revision/consultation workbench query contract | `/doctor-workflow/report-revision` and `/doctor-workflow/consultation` now both use case-scoped `GET /api/v1/pathology-cases/{id}/diagnostic-workbench` as the only read entrypoint, with frontend-loca… | Frontend `doctor-workflow` views, types, and service mappers must preserve the mixed context-row/list-row model and the `participants` detail shape on both `di… |
| DEC-20260616-001 | 2026-06-16 | M5 仪器设备管理旧系统对齐 | `/operation-resources/equipment` 按旧系统宽表契约扩展为“设备档案宽表 + 批量状态操作 + 前端导出/打印”页面：前端统一把 `equipmentCode` 展示为“资产编号”，复用现有设备详情/保养与预警能力，并通过 sibling backend `../SYBaseProject/bl-center` 的扩展设备档案字段和 `POST /api/v1/eq… | 前端设备表单、宽表列、详情展示、导出/打印工具、批量按钮与测试都必须围绕扩展字段工作；`nextMaintenanceAt` 继续只服务保养记录和预警，不并入旧系统主弹窗重点字段；导出/打印保持前端实现，不新增后端专用接口。后端记录对称决策为 `../SYBaseProject/DECISIONS.md#DEC-20… |
| DEC-20260616-002 | 2026-06-16 | M5 白片借记独立合同 | `/operation-support/borrow` 新增 `白片借记` tab，并通过 sibling backend `../SYBaseProject/bl-center` 独立消费 `GET /api/v1/white-slide-stocks`、`GET/POST /api/v1/white-slide-loans`、`POST /api/v1/white-slide-loans/{… | 前端必须保持白片独立 workspace、独立 service/types/打印工具；保存后可按“保存直接打印借阅单”走浏览器打印；读卡/配置按钮仅占位。后端记录对称决策为 `../SYBaseProject/DECISIONS.md#DEC-20260616-002`。 |
| DEC-20260616-004 | 2026-06-16 | M6 统计报表工作台与明细合同 | `/m6/custom-analysis` 保持现有路径和 `CustomStatisticsAnalysis` route name，但作为“统计报表工作台”单入口承载工作量、质量与安全、关键质控、冰冻时效、更改报告、不合格标本、自定义统计 tab；不新增后端菜单/权限种子。Current sibling backend `../SYBaseProject/bl-center` exposes… | M6 statistics pages should reuse `stat-reports/query/export` plus `stat-reports/details/query/export`; report rows may render metric status, numerator/denomina… |
| DEC-20260616-005 | 2026-06-16 | M6 工作量报表周期趋势合同 | `/m6/custom-analysis` 工作量 tab 通过同一个 `POST /api/v1/stat-reports/query` 合同传递 `periodMode=month | year`，并消费后端在 `OPERATION`/`WORKLOAD`指标行上返回的`trendPoints`。前端继续合并 `OP_CASE_VOLUME`、`OP_BILLING_AMOUNT`、`OP_PERFORMANCE_WORKLOAD`、`WL_DIAGNOSTIC_TASK_COUNT`、`WL_ME… |
| DEC-20260616-006 | 2026-06-16 | M6 质量指标趋势、分布与明细合同 | `/m6/custom-analysis` 质量相关 tab 优先消费后端质量指标行上的 `trendPoints` 和 `breakdowns`，并通过 `POST /api/v1/stat-reports/details/query` / `/details/export` 打开分页明细和明细导出；前端 service 必须保留这些字段，不得把质量图表退化为纯前端占位。 | `ReportChartPanel` 的趋势图优先使用 `trendPoints`，原因/状态图优先使用 `breakdowns`；明细模型继续保持非敏感字段，不新增旧 `/stat-report-details/*` 路径或临时 dashboard/detail 假接口。后端记录对称决策为 `../SYBasePr… |
| DEC-20260616-007 | 2026-06-16 | M6 工作量导出与展示一致性 | `/m6/custom-analysis` 工作量 tab 的导出必须与页面当前合并结果一致，因此前端使用本地合并 rows 生成 CSV，而不是只导出单一 `WORKLOAD` 后端结果。质量与自定义统计继续使用后端 `stat-reports/export`。 | 工作量导出文件名仍沿用统计报表命名规则，但文件内容由当前前端 merged rows 生成；其他 tab 保持后端导出路径不变。 |
| DEC-20260616-008 | 2026-06-16 | M6 关键质控危急值只读统计合同 | `/m6/custom-analysis` 关键质控 tab 固定展示 `QC_FROZEN_PARAFFIN_MATCH_RATE`、`QC_CRITICAL_VALUE_COUNT`、`QC_CRITICAL_VALUE_REPORT_TIMELINESS_RATE`、`QC_CRITICAL_VALUE_REASON_ANALYSIS_COUNT`；新增危急值三项来自 sibling ba… | 前端关键质控 tab 不再用不合格标本或诊断及时率替代危急值；原因/状态分布图优先选危急值原因分析 breakdowns；`UNAVAILABLE` 行仅显示“暂无明细”。Backend records the symmetric decision as `../SYBaseProject/DECISIONS.md#… |
| DEC-20260616-009 | 2026-06-16 | M6 Goal 6-8 report workbench indicators | `/m6/custom-analysis` 的冰冻时效、更改报告、不合格标本 tabs 固定消费 sibling backend `../SYBaseProject/bl-center` `V98__seed_m6_goal6_8_stat_indicators.sql` 种子的质量指标：冰冻诊断及时率/总超时/取材超时/切片超时/诊断超时，报告更改数量/医生数/修改原因/修订原因，不合格标本数… | 前端 tab 白名单必须使用 Goal 6-8 专用指标；后端 `sourceNote` 返回默认冰冻 SLA 30 分钟及真实字段/代理口径说明；Goal 9 的技术工位号、消息确认、危急值原因填写仍是后置跨模块 Full Workflow。Backend records the symmetric decisio… |
| DEC-20260616-003 | 2026-06-16 | M5 医疗废物管理双 Tab 与最小闭环合同 | `/operation-resources/medical-waste` 不再使用占位页，而是固定为 `人体标本` / `药物试剂` 双 Tab 的真实页面，并继续复用 `M5_RESOURCE_PAGE_AUTHORITIES`，不新增独立废物权限码。人体标本通过 sibling backend `../SYBaseProject/bl-center` `medical-waste` 合同完成… | 前端必须保持 route/menu/fallback/backend-menu 全部指向真实页面，medical-waste 类型/service 继续落在 `operation-support` 模块内；后端 `medical-waste` 模块与表结构成为稳定跨仓合同。药物试剂本轮只做到保存打印与一次交接，不扩展… |
| DEC-20260616-010 | 2026-06-16 | 系统用户角色展示依赖后端无前缀角色主数据 | 系统管理里的“分配角色”弹窗、用户列表角色列、角色管理页等角色名称展示继续直接使用 sibling backend `../SYBaseProject/bl-center` `/api/v1/roles` 和 `/api/v1/system-users` 返回的 `roleName`，不在前端单独剥离 `M2/M3/M4` 前缀。内置工作流角色的无前缀中文岗位名由后端主数据和 Flyway `V… | 前端新增/维护角色展示时应把后端 `roleName` 视为唯一展示源；若角色前缀需求再次变化，应优先在 backend `roles.role_name` 主数据层处理。Backend records the symmetric master-data decision as `../SYBaseProject/D… |
| DEC-20260617-003 | 2026-06-17 | M2 标本下游与 M5 归档患者信息语义 | 后续流程页面中的“病人ID”一律表示患者身份标识 `patientId`，不得再回退或展示 `caseId`；标本下游患者成组信息统一按 `patientId / inpatientNo / wardName / patientGender` 语义透传，前端允许在真实值缺失时显示 `-`，但不得用错误字段或硬编码占位掩盖可用数据。 | Frontend specimen-workflow pages now share one fallback matrix: list row native fields first, then application detail/workbench cache for补数；archive/borrow view… |
| DEC-20260617-005 | 2026-06-17 | M2 条码首次绑定脱离流程闸门 | 固定与转运页的首次 `条码绑定` 不再受标本当前流程状态限制：只要该标本当前未绑定且目标条码合法唯一，前端仍可在任意时点提交 `POST /api/v1/specimens/{specimenId}/barcode-binding`。`取消绑定` 与 `重绑` 继续沿用原有流程锁，不在本轮放开。 | Frontend runtime logic remains unchanged because `SpecimenBarcodeBindingPanel` already gates only on“单选未绑定行 + 输入目标条码”; the durable contract change lives in sib… |
| DEC-20260617-006 | 2026-06-17 | M3 制片管理日期范围统一合同 | 制片管理当前可见菜单页统一改为参照 `SpecimenReceiptView` 的 `daterange` 查询控件：默认空范围、不预填当天、仅在用户显式选择后才发送日期条件；任务列表类继续发 `createdFrom/createdToExclusive`，切片、包埋汇总、技术追踪、病理医嘱待办统一迁移到 `dateFrom/dateTo`，路由层仅保留旧 `workDate` 深链兼容并在读… | Visible technical-workflow pages must render the same range placeholders/separator/unlink-panels behavior as `SpecimenReceiptView`; service/query helpers must … |
| DEC-20260617-007 | 2026-06-17 | M2 申请登记工作台标本字典科室过滤合同 | `/workflow/application-registration-workbench` 的标本字典统一改为消费 sibling backend `../SYBaseProject/bl-center` `GET /api/v1/application-registration-workbench/specimen-dictionary`，由后端按当前登录用户 `departmentId` … | Frontend `application-registration-workbench-service` / `useApplicationRegistrationWorkbenchSpecimens` 现在以同一份后端字典响应驱动搜索、系统列表、部位列表、标本项追加入口和两个常用区块；两个常用区块都只允许显示过滤… |
| DEC-20260617-008 | 2026-06-17 | 全站可见“病人ID / ID号”展示口径 | Visible patient ID labels now prefer application-registration workbench `patientInfo.idNo`, surfaced as `patientIdDisplay`; internal `patientId` remains the backend patient/entity identifier and quer… | Frontend display formatters and page slots must use `patientIdDisplay / patientInfo.idNo` before falling back to `patientId` or `-`; backend sibling `../SYBase… |
| DEC-20260618-001 | 2026-06-18 | 标本字典主数据迁移到系统配置 | `/system/configs` 现在内置“标本字典”专用管理区，前端通过 sibling backend `../SYBaseProject/bl-center` `GET /api/v1/system-configs/specimen-dictionary` 与对应 categories/items CRUD 维护“系统 -> 部位 -> 标本项”三层字典；科室关联固定到标本项级 `dep… | Frontend `SystemConfigsView` 在命中 `SPECIMEN_DICTIONARY` 根分类时切换到专用工作区；系统配置模块新增标本字典树/项类型和 API；采集页 `application-registration-workbench-service` / `useApplicationRe… |
| DEC-20260618-002 | 2026-06-18 | 手术楼/手术室选项统一改为系统配置主数据 | `标本采集` 相关页面继续调用 `GET /api/v1/application-registration-workbench/operating-options`，但该接口的语义改为读取 sibling backend `../SYBaseProject/bl-center` 系统配置里的 `WORKFLOW_REFERENCE -> OPERATING_ROOM -> 楼栋 -> 手术室项`… | Frontend `application-registration-workbench-service`、`useApplicationRegistrationWorkbench`、`operating-room-display` 及相关工作台/签收/确认/固定展示链路必须把 `operating-options`… |
| DEC-20260618-003 | 2026-06-18 | Specimen workbench frozen reminder UI/polling | The top "冰冻提醒" checkbox in the specimen collection workbench changes from a read-only display field to a per-session reminder toggle. Its initial value follows the loaded application record's `patien… | Affected paths: `apps/web-ele/src/modules/specimen-workflow/components/ApplicationRegistrationWorkbenchToolbar.vue`, `ApplicationRegistrationWorkbenchPanel.vue… |
