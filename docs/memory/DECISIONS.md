# DECISIONS.md

## Purpose

Track durable frontend and cross-repo decisions that future agents must preserve.

## Decisions

| ID | Date | Context | Decision | Rationale | Impact | Revisit When |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| DEC-20260608-001 | 2026-06-08 | AI memory layer | Maintain repo-local memory files in both frontend and backend repos; update them at task delivery time on demand. | Keeps durable context close to the code while avoiding noisy hook-enforced updates. | Future AI tasks must check and update memory files before final handoff. | If automatic agent dispatch or external memory sync is introduced. |
| DEC-20260608-002 | 2026-06-08 | Dynamic workflow | Use Workflow Packet templates instead of an automatic dispatcher. | Keeps the workflow auditable and low-maintenance while still routing tasks by type. | PRs must explain chosen Workflow, agents, tests, simulation, security/DB checks, and Red Team outcome. | If team asks for script-assisted routing. |
| DEC-20260608-003 | 2026-06-08 | Governance doc consistency audit | `AGENTS.md` section 8 is the single source of truth for memory-file roles and generic update triggers; `DYNAMIC_WORKFLOW_RULES.md` and `GIT_RULES.md` cross-reference it instead of restating. `DYNAMIC_WORKFLOW_RULES.md` is now a first-class governance doc in the AGENTS reading order, mapping table, related-docs, and `docs/README.md`. `check:cspell` glob widened to `.ts/.tsx/.vue`. | Triple-maintained memory rules drifted; the dynamic workflow doc was undiscoverable; cspell silently skipped `.vue/.tsx`. | Future memory-rule edits change only AGENTS section 8; other docs keep references. Widened cspell may surface new spelling warnings needing the project dictionary. | If memory rules move out of AGENTS or a routing dispatcher is added. |
| DEC-20260608-004 | 2026-06-08 | M2 application registration workbench | When an `APPLICATION_NO` lookup misses, the frontend reuses the existing application create API, generates editable placeholder patient/workbench fields, saves them through the workbench patient-info API, and then opens the record for registration. | Registration users need barcode-style application number entry to always land in an editable workbench record while preserving existing backend APIs and validation rules. | Auto-create depends on `PERM_APPLICATION_CREATE`; non-application-number lookup types do not create records to avoid treating names, inpatient numbers, or route IDs as application numbers. | If backend adds a dedicated lookup-or-create workbench endpoint or changes application create validation. |
| DEC-20260608-005 | 2026-06-08 | Technical specimen registration pathology number editing | The frontend sends an optional completion-time `pathologyNo` candidate with `applicationType`; non-empty candidates are not auto-completed by the backend and must already match the selected type and be unique. Empty candidates let the backend generate the final number. | Keeps the workstation editable while preserving backend authority over numbering validity and uniqueness. | Frontend previews are candidate values only; users see/type the draft beside “送检标本”, and backend 400/409 responses are the source of truth. | If backend adds a dedicated reserve/preview numbering endpoint or changes numbering rule semantics. |
| DEC-20260608-006 | 2026-06-08 | Slicing workstation print-before-slice flow | The slicing workstation treats backend-confirmed slide printing (`POST /api/v1/slicings/slide-print`) as the required transition before a task appears in the slicing tab; `completeSlicing` no longer creates slide numbers or accepts `slideCount`. | Slide labels must be generated, optionally adjacent-merged, and marked printed before physical slicing work is recorded. | Frontend uses `pendingPrintList` for `玻片打印`, `pendingSliceList` for `切片`, and sends `applicationType=ROUTINE/FROZEN` filters that affect backend statistics and all lists. | If a dedicated print gateway rollback/reservation flow is added or repeated merge/print becomes allowed. |
| DEC-20260608-007 | 2026-06-08 | Tracking and abnormal application list pathology number display | Use backend `GET /api/v1/applications` `items[].pathologyNo` as the single source for the “病理号” column; frontend does not fan out detail requests to fill the list. As of 2026-06-17, the same endpoint also accepts optional `pathologyNo` fuzzy filtering for the tracking application tab. | Keeps the list performant and avoids N+1 tracking/detail queries while still supporting direct pathology-number lookup from the tracking application list. | Backend and frontend list item/query contracts must stay aligned; empty values mean the application has not produced a pathology case yet and display as `-`. The M2 tracking application tab may query by application number, pathology number, patient name, and date range without touching the specimen tab. | If the application list supports multiple active cases per application or the pathology-number query semantics change from fuzzy to exact/normalized matching. |
| DEC-20260608-008 | 2026-06-08 | Historical-status lookup in M2/M3 workflow pages | Ordinary page entry remains empty, while explicit business-key queries may return all-status historical rows; action availability is still decided by each row's current workflow status. For M2 outbound, `identifier` is an exact specimen-number-or-barcode lookup and only queues a unique outbound-ready row on the frontend; it does not directly submit transport. | Users need to find old specimens/tasks by specimen/application/pathology/task identifiers without accidentally re-enabling current-stage operations on completed or out-of-scope records. Barcode scanners should behave like text input plus Enter while preserving outbound review before submit. | M2 specimen pages use existing status/reason display for old rows; M3 grossing/dehydration only pass backend `includeAllStatuses=true` for explicit query/deep-link contexts; backend `GET /api/v1/specimen-outbounds` and `GET /api/v1/technical-tasks/pending` must preserve this contract. | If product decides ordinary entry should preload pending rows again, if backend adds dedicated historical search endpoints, or if outbound scanning should submit transport immediately. |
| DEC-20260609-001 | 2026-06-09 | M2 specimen check-in and transport operator identity | The selected current login user may act as the check-in / outbound / handover operator without an `operatorVerificationToken`; selected non-current operators still require the existing operator verification prompt and token. Specimen confirmation remains on the existing verification-token path. | The current HTTP session already authenticates the logged-in user, while cross-user operation still needs explicit verification. | Frontend payload builders must omit `operatorVerificationToken` only for the current user; backend check-in / transport controllers must fall back to current request user only when token is blank and submitted user ID matches the current user. | If product requires every operator action, including current-user actions, to re-enter credentials. |
| DEC-20260609-002 | 2026-06-09 | M2 specimen barcode binding semantics | A specimen registered without an explicit barcode remains `UNBOUND` with `barcode=null`; `POST /api/v1/specimens/{specimenId}/barcode-binding` is the first-write path for `targetBarcode`. Frontend pages must treat null/blank barcode as not usable for barcode-based downstream operations. | Barcode binding is an operator action: users input the target barcode, select one unbound specimen, and click “条码绑定” before the barcode exists. Auto-generating `applicationNo-specimenNo` at registration made every new row appear already bound. | `SpecimenTrackingSummary.barcode` is nullable in frontend types; transport creation excludes unbound specimens; barcode binding table/export place “标本条码” immediately after “标本编号”. | If product reintroduces preprinted/generated barcodes before operator binding or adds a dedicated barcode reservation endpoint. |
| DEC-20260609-003 | 2026-06-09 | M3 embedding confirmation workflow | `POST /api/v1/embeddings/start` is the confirmation action and must persist `EMBEDDING_CONFIRM_PENDING` / “包埋确认待完成”; `POST /api/v1/embeddings/cancel` may return only that middle state to `PENDING` / 待包埋; only `POST /api/v1/embeddings/complete` writes final `COMPLETED` embedding records and creates downstream slicing tasks. Legacy `IN_PROGRESS` embedding tasks remain completable but are not cancellable through the rollback endpoint. | Product requires “确认包埋” and “确认包埋完成” to be separate durable states, with a pre-completion correction path that does not create or delete final embedding records. | Frontend top “确认包埋” must not call `completeEmbedding`; right-side “取消包埋” is enabled only for `EMBEDDING_CONFIRM_PENDING`; completion buttons are enabled only for `EMBEDDING_CONFIRM_PENDING` or legacy `IN_PROGRESS`; pending embedding lists and summary pending counts include the middle state, while completed lists and slicing entry remain final-completion only. | If backend renames the state code, product allows cancelling final completed embedding records, or a broader technical-task state machine is introduced. |
| DEC-20260609-004 | 2026-06-09 | M3 slicing workstation print controls | `SlicingWorkstationView` no longer exposes the marked print-parameter controls for slide count, adjacent merge/cancel merge, printer code, print remarks, pending-today filtering, or print-strategy helper copy; the visible print action submits fixed defaults. | The latest UI request explicitly removed those marked elements while keeping the print tab and `打印玻片` action. | Frontend `POST /api/v1/slicings/slide-print` calls from this page send `sourceSlideCount=1`, `mergeAdjacent=false`, and blank printer/remarks unless product reintroduces a visible parameter entry path. | If print quantity, merge strategy, printer routing, remarks, or today-only filtering become required user inputs again. |
| DEC-20260609-005 | 2026-06-09 | M3 slicing workstation slide print handoff | Clicking `打印玻片` opens a browser print window, supports multiple selected rows, writes generated slide labels into one print document, refreshes the workbench, and keeps the active tab on `玻片打印` after backend print completion. | Operators need to batch print labels and decide themselves when to move from printing to slicing; automatic tab switching hides the remaining print queue and interrupts batch-oriented work. | `SlicingWorkstationView` calls `POST /api/v1/slicings/slide-print` once per selected print row using the fixed defaults from `DEC-20260609-004`; frontend must not set `activeTab='slice'` in the print success path. | If product adds a print queue device integration, server-side batch print endpoint, or explicit post-print auto-navigation option. |
| DEC-20260609-006 | 2026-06-09 | M2 specimen workflow identifiers | 标本固定、标本确认、标本入库、标本签收、标本出库推进动作以 `specimenId` 为行级首选标识，保留 `specimenBarcode` / `specimenNo` 兼容扫码和旧调用；条码绑定仅是关联与扫码便利，不是这些流程的前置条件。 | 未绑定条码的真实标本仍需要推进业务流程；强制条码会把可由标本 ID 唯一定位的行操作错误拦截。 | Frontend workflow payloads send `specimenId` when the user acts on a row; receipt payloads send `specimenId > specimenBarcode > specimenNo`; transport creation sends `specimenIds`; nullable barcode fields must not disable these buttons. Sibling backend resolves `specimenId > specimenBarcode > specimenNo` and rejects ambiguous specimen-number requests. | If product redefines barcode binding as a compliance gate before fixation/confirmation/check-in/receipt/outbound. |
| DEC-20260609-007 | 2026-06-09 | M3 slicing pending slide-print merge groups | Pending slide-print merge is persisted server-side and scoped to same patient + same case/pathology number + same embedding-box letter prefix; the frontend only requests grouping/cancel/print and renders backend returned virtual rows. | 合片状态 must survive refresh and must never pair `A` with `B` or cross patient/case records. Server-side persistence also keeps odd-tail rows as ordinary pending print rows. | Frontend uses `POST /api/v1/slicings/slide-print-merge-groups`, `/cancel`, and `/print`; merged workbench rows use `printGroupId`, `mergedPrintGroup`, `taskIds`, `embeddingBoxIds`, and `embeddingBoxNo` such as `A1+A2`. | If product allows manual arbitrary multi-block merge, printed-group cancellation, or a single physical-slide backend print result instead of per-item slide records. |
| DEC-20260610-001 | 2026-06-10 | Dynamic workflow effectiveness | Wire the dynamic-workflow routing (kept as templates per `DEC-20260608-002`) into the per-task loop instead of leaving it doc-only: `AGENTS.md` task-start template now requires a `主 Workflow` + modifiers field, delivery output requires a slim Workflow Packet, the checklist adds Workflow/Red Team items, and the mapping-table trigger wording was made concrete. `DYNAMIC_WORKFLOW_RULES.md` gains a trigger-signal lookup table (path/signal → primary Workflow → required modifiers). | The routing was effectively unused because it never appeared in the templates agents actually execute each turn (unlike AI Memory Update), and selection relied on subjective judgement. Anchoring it in the start/delivery/checklist loop plus a lookup table makes classification reproducible without an automatic dispatcher. | Every implementation task must declare a primary Workflow + modifiers at start and a slim Workflow Packet at delivery; doc/chat tasks may mark `不适用`. No CI/hook enforcement was added (option C deferred, red-zone). Extends `DEC-20260608-002`/`DEC-20260608-003`. | If a CI gate validating the PR Workflow Packet is added, or an automatic routing dispatcher is introduced. |
| DEC-20260610-002 | 2026-06-10 | M5 archive and borrow page organization | Keep `归档管理` and `借记管理` as separate menu pages and organize each page internally with tabs; the archive page owns application-form/蜡块/slide/cabinet/record work, while the borrow page owns slide/蜡块 borrow and pending-return/return work. UI text may say “蜡块”, but the frontend must keep using the existing backend `EMBEDDING_BOX` material/object type and `/v1/archive/embedding-boxes` contract. | The current backend already supports archive and material-loan contracts, while old-system gaps such as specimen archive, photo archive, white-slide borrow, scanner, print, and export need separate contract work instead of being mixed into the tab split. | Future M5 frontend work should not re-merge borrow controls into the archive page or add unsupported old-system capabilities without a new backend/API/DB/external-device task. Linear issues `SID-76` through `SID-79` track the tab split, borrow page, permission-route tests, and gap research. | If backend introduces dedicated specimen/photo/white-slide/scan/print/export contracts or product changes the menu split. |
| DEC-20260610-003 | 2026-06-10 | 全站页面级标题头样式 | `@vben/common-ui` `Page` 新增显式 `showHeader` 开关，`apps/web-ele` 现有页面统一传 `show-header=false`，移除页面级大标题/说明头，仅保留正文、卡片内标题、面包屑、标签页与业务工具区。 | 当前项目大量页面在 `Page` 头部重复展示模块名和说明文案，信息密度偏低，且与页内卡片结构重复。用显式开关而不是改默认值，可避免影响共享包和未来需要页头的个别场景。 | 后续新页面默认应沿用无页头模式；只有明确需要页面级头部时才显式保留或开启 `showHeader`。 | If product重新要求恢复页面级标题头，或某类页面需要统一复用 `Page` 头部承载跨页操作区。 |
| DEC-20260610-004 | 2026-06-10 | 系统日志管理与操作审计 | 系统管理只新增单菜单 `/system/logs`，页面内用 登录日志 / 操作日志 两个 tab；前端查询入口统一走 sibling backend `../SYBaseProject/bl-center` 的 `/api/v1/system/logs/*`。操作审计定义为所有已认证、带权限注解的写操作，加上显式 `@AuditOperation(sensitiveQuery = true)` 的敏感查询。 | 避免把登录日志和操作日志拆成多个菜单，同时用横切审计覆盖写操作，不把普通列表/字典查询变成高噪声审计流。 | 前端路由 authority 使用 `PERM_SYS_LOG_QUERY`，详情请求依赖后端 `PERM_SYS_LOG_DETAIL`；日志页本身属于敏感查询，会被审计但不得递归写日志；列表不展示长操作内容，详情展示后端脱敏截断内容。 | If product requires export/retention cleanup, ordinary read-query auditing, or moving log query ownership out of `bl-center`. |
| DEC-20260611-001 | 2026-06-11 | AI skill routing | 外部 skills 作为 `AGENTS.md` 和 `DYNAMIC_WORKFLOW_RULES.md` 下的可选专家/执行补强，安装到 Codex 用户级目录，项目内只维护 `docs/AGENT_SKILL_ROUTING.md` 映射，不复制第三方 skill 源码。 | 避免 Superpowers、Matt Pocock skills、Addy Osmani agent-skills 与项目规范形成多套总调度，同时让 UI/API/Security/Debug/Review 等专项能力可复用。 | Future agent tasks should select the primary Workflow first, then choose at most a small number of optional skills from the routing doc; external skills must not override red-zone confirmation, validation, Backend Cross-check, or Memory Update rules. | If an automatic skill dispatcher is introduced, or if the team decides to vendor third-party skills into the repo. |
| DEC-20260611-002 | 2026-06-11 | M5 archive cabinet maintenance | `归档柜列表` is a real full-stack tree-table workflow, not a screenshot-only placeholder: frontend calls sibling backend `POST /api/v1/archive-cabinets/batch` for prefix/serial batch creation and `DELETE /api/v1/archive-cabinets/{id}` for deletion. Delete remains empty-only and also rejects cabinets with storage/loan position references. | Users need bulk cabinet setup and cleanup from the archive page, while preserving archive history and avoiding fake unsupported specimen/white-slide capabilities. | Frontend route authorities include `PERM_M5_ARCHIVE_CABINET_DELETE`; batch create reuses create permission; no `SPECIMEN` / 标本柜 type is introduced. Backend `V85__add_m5_archive_cabinet_delete_permission` grants delete to archive/admin roles. | If product allows deleting historical cabinet references, adds specimen cabinets, or introduces scan/print/export contracts for M5 archive cabinets. |
| DEC-20260611-003 | 2026-06-11 | M5 reagent inventory/template contract | `/operation-resources/reagents` is organized as two tabs: `试剂库存` for stock batches and `试剂模板` for reusable reagent definitions. Sibling backend `../SYBaseProject/bl-center` owns the durable split where `reagents` is the template table, `reagent_stocks` is the inventory batch table, and `reagent_stock_events` is the authoritative action/audit trail. CSV import/export uses UTF-8 BOM CSV while UI labels the feature as Excel; reagent request bodies omit legacy `operatorName` / `operatorUserId`; existing M5 reagent permission codes are reused. | The old desktop workflow mixes template, batch, test, consume, and audit fields; splitting template from inventory gives a stable API/data boundary without adding a new permission model or xlsx dependency. Authenticated backend context is the trustworthy source of operator identity. | Frontend service/types/dialogs/tests must keep the template-vs-stock distinction, consume stock action/event endpoints, and treat CSV row errors as backend-returned import results. Backend changes live behind migration `V86__extend_reagent_inventory_legacy_fields` and the extended `/api/v1/reagents` / `/api/v1/reagent-stocks` contracts. | If product requires real `.xlsx`, per-action permission codes, hard deletion of templates, or a different medical-order mapping source than `medical_order_items.id`. |
| DEC-20260611-004 | 2026-06-11 | M5 archive object list contract | `/operation-support/archive` object tabs must use sibling backend `GET /api/v1/archive-objects` for paged object全集 by `APPLICATION_FORM`, `EMBEDDING_BOX`, or `SLIDE`; `GET /api/v1/archive-records/search` remains only for archive-record/history-style consumers. | Storage records only represent archived objects, so using archive-record search as the tab data source hides unarchived application forms, wax blocks, and slides. | Each archive object tab owns independent `keyword/page/size/total/items/loading/error` state and refreshes the current tab after archive success. The new endpoint reuses `PERM_M5_ARCHIVE_QUERY`; frontend must not infer unsupported old-system fields and should display `-` where the contract has no field. | If backend adds specimen/photo/white-slide archive object types, scanner/print/export flows, or changes the archive object pagination contract. |
| DEC-20260611-005 | 2026-06-11 | AI automatic commit granularity | AI commits use acceptance-point grouping plus low/medium/high risk ratings. AI must explain proposed commit groups before committing; low and medium risk groups may be auto-committed after that explanation, while high risk groups require explicit user approval. | The project needs fast AI handoff for routine work without letting dirty worktrees, red-zone changes, or mixed ownership slip into commits. Risk-rated grouping makes commit autonomy auditable while preserving a hard pause for high-impact changes. | Future AI commit flows must list included/excluded files, verification evidence, risk rating, risk rationale, and rollback impact before staging. Path-limited staging is mandatory; unclear ownership or inseparable mixed edits escalate the group to high risk. | If a machine-enforced commit grouping tool, hook, or CI gate is introduced, or if the team decides all AI commits require manual approval. |
| DEC-20260611-006 | 2026-06-11 | Loop Engineering governance | Introduce `docs/LOOP_ENGINEERING_RULES.md` as a lightweight loop entrypoint with Task Intake, Implementation, Review, and Triage loops; PR/Linear/Codex Goal templates now include Loop Packet fields; `.github/workflows/pr-packet.yml` runs `scripts/validate-pr-packet.mjs` to reject PR bodies missing key Workflow/validation/risk/Memory fields. | The project already had strong workflow documents but many requirements were soft and easy to skip. Loop Packet captures how an agent loop runs and stops, while the PR-body CI gate turns the most basic packet completeness checks into a machine-enforced signal without introducing an automatic dispatcher. | Future loop-driven tasks must state loop type, stop condition, verification command, state sink, and escalation condition. `pr-packet.yml` enforces field presence only; reviewer judgment remains required for evidence quality, Red Team substance, and human confirmation. Partially addresses `TD-20260610-002`. | If the team adds a trusted PR-body validator that runs from the base branch, introduces automatic workflow dispatch, or decides draft PRs should be exempt from packet validation. |
| DEC-20260611-007 | 2026-06-11 | M5 specimen archive contract | `SPECIMEN` is now a supported M5 archive object type with independent permission `PERM_M5_SPECIMEN_ARCHIVE`, frontend tab `标本归档`, backend `POST /api/v1/archive/specimens`, and object pagination through `GET /api/v1/archive-objects?objectType=SPECIMEN`. It is not a material-loan type. | The old-system requirement needs real specimen archive behavior, but borrowing rules still apply only to wax blocks and slides. Keeping a separate permission preserves least privilege and backend enforcement. | `/operation-support/archive` can show and archive specimens; archive dialog/buttons must check specimen permission; borrow pages and material-loan APIs must not accept `SPECIMEN` unless a separate borrowing contract is designed. Supersedes the “no specimen archive without contract” part of `DEC-20260610-002` and `DEC-20260611-002`. | If product adds specimen borrowing, photo archive, barcode scan archive, print/export, or separate specimen cabinet taxonomy. |
| DEC-20260611-008 | 2026-06-11 | M6 statistics report contract | Frontend statistic report queries use `workloadUserId` for workload/operator filtering and must not send legacy `operatorUserId` / `operatorName`. Query responses may include optional metadata (`metricStatus`, `numerator`, `denominator`, `trendPoints`, `breakdowns`, `sourceNote`); missing or uncertain metrics are represented by backend `UNAVAILABLE` / `PARTIAL` statuses instead of invented values. CSV export remains UTF-8 BOM CSV with the legacy four columns. | Keeps the M6 contract backward-compatible for existing sparse report rows while making metric availability and data provenance explicit. It also preserves the confirmed no-`xlsx` export scope. | `apps/web-ele/src/modules/m6-statistics` should tolerate old sparse rows and display/use extended metadata when pages add richer cards or drilldowns. Sibling backend `../SYBaseProject/bl-center` owns metric availability classification and source notes. | If backend adds authoritative sources for currently unavailable quality indicators, or product requires `.xlsx` / different export columns. |
| DEC-20260611-009 | 2026-06-11 | M6 statistics dashboard contract | `/m6/dashboard` is the default M6 statistics landing page. It reuses the existing statistic report query permission scope instead of adding a new permission model; backend menu seed uses `M6_SUPPORT` as the root and `M6_DASHBOARD` for the child route. The original aggregate-endpoint assumption was superseded on 2026-06-12 by `DEC-20260612-002`, which corrected the current implementation to compose report queries instead of calling `POST /api/v1/stat-dashboard/query`. | The dashboard summarizes existing metric computations and availability status, so it should not create a separate authorization surface or duplicate frontend-only metric logic. Aligning to backend `M6_SUPPORT` avoids fake `M6_STATISTICS` menu codes in dynamic routing tests. | Frontend `M6_ROUTE_ITEMS`, static route, fallback menu route, and backend-menu mapper should point statistics users to `/m6/dashboard`; service/page code must keep report analysis under `/m6/custom-analysis` and follow the current endpoint rules in `DEC-20260612-002`. | If product requires independent dashboard permissions, a different M6 root menu code, or drilldown cards with new backend contracts. |
| DEC-20260611-010 | 2026-06-11 | M6 quality indicator overview and export | `/m6/quality-indicators` is a real quality-statistics overview page, not a placeholder. It queries `POST /api/v1/stat-reports/query` with `category=QUALITY`, surfaces `AVAILABLE` / `PARTIAL` / `UNAVAILABLE`, numerator/denominator, and `sourceNote`, and exports the current overview as UTF-8 BOM CSV with `indicatorCode,indicatorName,metricValue,metricUnit,metricStatus,numerator,denominator,sourceNote`. | Quality metrics with missing sources must be visibly unavailable instead of silently empty or approximated, and export users need the same provenance/status fields they see on screen while keeping the confirmed CSV/no-`xlsx` scope. | Supersedes the export-column portion of `DEC-20260611-008`; sparse query rows remain compatible, but report CSV consumers must now tolerate the extended column set. Frontend must keep using backend-provided status/source fields and must not compute fake quality values. | If product requires `.xlsx`, export-template selection, or backend adds authoritative sources for currently unavailable quality indicators. |
| DEC-20260611-011 | 2026-06-11 | M6 quality detail drilldown contract | `/m6/quality-indicators` currently has no sibling-backend quality-detail query/export endpoint. The frontend may keep a quality detail drawer, but it must not call `/api/v1/stat-report-details/query` or `/export` unless the sibling backend first adds a real contract. Until then, the drawer/export path should only present non-sensitive availability guidance keyed by the existing `detailType` values (`REPORT_REVISION`, `UNQUALIFIED_SPECIMEN`, `FROZEN_TIMEOUT`, `CRITICAL_VALUE_REASON`). | Cross-repo verification on 2026-06-16 showed sibling backend `../SYBaseProject/bl-center/src/main/java/com/company/bl/interfaces/controller/M6ManagementController.java` exposes only `/api/v1/stat-reports/query` and `/export`; the previous detail-endpoint assumption caused user-facing 404s. A safe degraded drawer avoids reintroducing nonexistent APIs or leaking sensitive placeholders. | Superseded by `DEC-20260616-004`: current sibling backend now exposes `/api/v1/stat-reports/details/query` and `/details/export`; the old mistaken `/api/v1/stat-report-details/*` path remains forbidden. | Historical only; use `DEC-20260616-004` for current implementation. |
| DEC-20260611-012 | 2026-06-11 | M6 management indicator statistics | `/m6/management-indicators` is a real management-statistics page that composes two report queries: `category=OPERATION` for business volume/billing/material warning metrics and `category=WORKLOAD` for performance, diagnostic task, workload, and medical-order metrics. Personnel filtering uses `workloadUserId`; role and department filters are passed through the existing statistic report filter model. | Management indicators reuse the backward-compatible report contract and backend availability semantics without creating a separate endpoint or frontend-only calculations. This keeps unsupported metrics visible as backend `UNAVAILABLE` / `PARTIAL` instead of approximated. | Frontend must keep management statistics under `apps/web-ele/src/modules/m6-statistics`, reuse `queryStatReport` / `exportStatReport`, and avoid legacy `operatorUserId` / `operatorName` fields. Backend `../SYBaseProject/bl-center` remains authoritative for metric grouping, status, and source notes. | If product requires independent management-statistics permissions, drilldown endpoints, or different export schemas for operation/workload reports. |
| DEC-20260612-001 | 2026-06-12 | M6 dynamic menu closure and internal CSS dependency ownership | Backend-menu component definitions and static fallback menu routes are part of the M6 page contract: `/m6/quality-indicators` and `/m6/management-indicators` must resolve to their real statistics views, not placeholders. Internal packages that export CSS with `@import` / `@plugin` statements must declare those imported packages as direct dependencies in the owning package. | Static route tests alone did not catch backend-menu/fallback placeholder drift, and clean worktrees exposed hidden dependency assumptions in `@vben/tailwind-config`. | M6 menu tests should assert dynamic/fallback component paths for real pages. `internal/tailwind-config/package.json` directly depends on `tw-animate-css`, `@iconify/tailwind4`, and `@tailwindcss/typography`; future CSS/plugin imports in internal packages need the same dependency ownership. | If the route/menu source is redesigned, or if the package manager/build tool gains a documented shared-plugin resolution layer. |
| DEC-20260612-002 | 2026-06-12 | M6 dashboard endpoint contract correction | `/m6/dashboard` must not call or alias `POST /api/v1/stat-dashboard/query`; current sibling backend `../SYBaseProject/bl-center` has no such route. The dashboard composes `POST /api/v1/stat-reports/query` for `QUALITY`, `OPERATION`, and `WORKLOAD`, then derives summary cards client-side from preferred indicator codes. | User-reported backend access log confirmed the dashboard aggregate route returns 404, while the report query endpoint is the stable M6 statistics contract already protected by report query permissions. Reusing report queries avoids adding a phantom frontend/backend contract. | Frontend service tests must keep asserting three `/v1/stat-reports/query` calls and no `/v1/stat-dashboard/query`; backend-menu aliases should use `/m6/dashboard` for the page and leave `/api/v1/stat-reports/query` mapped to custom analysis/report consumers. This supersedes the endpoint portion of `DEC-20260611-009`. | If backend later adds an implemented, permissioned dashboard aggregate endpoint with matching tests and migration/menu seed. |
| DEC-20260612-003 | 2026-06-12 | Karpathy guidelines governance integration | Integrate the Karpathy-inspired principles as project-specific checks inside existing governance docs: task confirmation now records success criteria and non-goals, coding rules emphasize simple current-scope implementations and testable fixes, AI health checks call out silent assumptions, unrelated edits, and speculative abstractions, and skill routing treats Karpathy guidance as a behavior baseline only. | The project already has authoritative workflow, risk, validation, and memory rules. A separate vendored guideline file or plugin would create a parallel rule system and duplicate existing checks. | Future AI tasks should use “clarify first, keep changes surgical, avoid speculative abstraction, verify against success criteria” through `AGENTS.md`, `CODING_RULES.md`, and `AI-CODE-HEALTH.md`; do not vendor `andrej-karpathy-skills` or introduce it as a new dispatcher. | If the team decides to install external skills into project scope or replace current workflow routing with a different governance system. |
| DEC-20260612-004 | 2026-06-12 | Governance closure baseline | Add a lightweight local governance validator (`pnpm run check:governance`) that checks duplicate `DEC-*` IDs, required top-level entries in `docs/README.md`, and stale `ARCHITECTURE.md` references to the removed M6 dashboard aggregate endpoint. | The current governance failure modes are concrete drift issues, not missing process prose: duplicate memory IDs, docs index drift, and stale architecture facts were already present. A narrow validator raises signal without turning governance into a heavyweight platform project. | Future repo-wide `pnpm check` runs now include `check:governance`; governance automation remains intentionally narrow and does not replace reviewer judgment on Workflow Packet quality, Red Team substance, or human confirmation. | If the team broadens governance lint to cover more memory files, markdown quality, or PR/body cross-checks. |
| DEC-20260612-005 | 2026-06-12 | Governance ergonomics for entry context and Linear isolation | `PROJECT_STATE.md` is a current-state summary, not a historical execution log; keep it short and push deep validation history into PRs, bug/debt ledgers, or git history. Linear implementation tasks still default to isolated `git worktree`, but low-risk exceptions are allowed for pure docs/audit work or single green-zone micro changes in a clean workspace when the reason is documented. | The project already had the right safety goals, but entry context had grown too large to serve as an effective handoff file, and the fully rigid worktree rule added friction for low-risk governance/doc tasks without meaningfully reducing risk. | Future sessions should treat `PROJECT_STATE.md` as the first-read summary and use worktree exceptions only when the task remains clearly low-risk and non-parallel. As soon as scope expands into runtime/shared/dependency/build changes, worktree isolation becomes mandatory again. | If the team decides every Linear issue must always run in a worktree regardless of task type, or if a better archival place for historical validation detail is introduced. |
| DEC-20260612-006 | 2026-06-12 | PROJECT_STATE governance guardrail | Extend `pnpm run check:governance` to validate that `PROJECT_STATE.md` keeps the required summary sections and stays within a generous line budget, so the handoff file remains a current-state entrypoint rather than regressing into a historical log. | The new short-form `PROJECT_STATE.md` solves today's problem, but without a minimal automated guard it is likely to regrow through future well-intentioned validation dumps. Section and size checks are stable enough to enforce the file's role without coupling to exact wording. | Future changes to `PROJECT_STATE.md` must preserve its five summary sections and keep detailed validation history elsewhere unless the line budget is intentionally revisited. | If the team replaces `PROJECT_STATE.md` with a different handoff format or needs a different size budget. |
| DEC-20260612-007 | 2026-06-12 | Governance entrypoint consistency and check coverage | Keep `AGENTS.md` and `docs/CODING_RULES.md` aligned with the active governance command set by explicitly documenting `pnpm run check:governance`, and have the local validator check both `AGENTS.md` and `docs/README.md` for the full top-level doc index so the entrypoint path does not drift again. | The biggest remaining friction was not missing rules, but stale entrypoint documentation: people could follow one file and miss the new governance gate or a relocated top-level doc. A narrow validator plus explicit command docs fixes that without broadening governance into a heavy lint system. | Future edits to governance docs should treat `AGENTS.md`, `docs/README.md`, and `docs/CODING_RULES.md` as a single entrypoint surface. `check:governance` stays narrow and local, and reviewer judgment still decides whether the docs are actually useful. | If the docs entrypoint layout changes again, or if governance validation grows into broader markdown/content linting. |
| DEC-20260612-008 | 2026-06-12 | Governance Phase A/B/C closure | Governance rules now use explicit single-source boundaries: Workflow/Red Team/Workflow Packet semantics come from `docs/DYNAMIC_WORKFLOW_RULES.md`, worktree and dirty-workspace isolation comes from `docs/GIT_RULES.md` §6, and Memory Update trigger decisions come from `AGENTS.md` §8. Low-risk pure docs/audit/read-only tasks may use simplified delivery and `Primary Workflow: Not applicable`, while implementation workflows keep the full packet. Phase C adds a narrow `governance-quality.yml` CI and extends PR packet validation so declared Red Team usage must include at least `Attack result` and `Residual risk`. | The audit showed the main execution failures were multi-document ambiguity, low-risk formal overhead, and machine checks that only verified field presence. Explicit boundaries plus a narrow docs-only guardrail reduce confusion without dragging governance changes through full frontend CI or trying to automate subjective review. | Future governance edits should preserve the “single source + light guardrail” shape: avoid re-defining Workflow/worktree/memory rules in multiple docs, keep low-risk tasks lightweight, and only automate stable low-dispute checks. Reviewers remain responsible for evidence quality, Checker substance, and human confirmation. | If the team wants stricter PR/body evidence automation, broader governance lint, or a different docs-only CI strategy. |
| DEC-20260612-009 | 2026-06-12 | Governance audit-driven optimization (Phase D) | Acting on a multi-dimensional audit: the modifier taxonomy全集 (incl. Browser Verification / Backend Cross-check) is defined only in `docs/DYNAMIC_WORKFLOW_RULES.md` 总规则; `docs/GIT_RULES.md` §8 collapses to a pointer; validation commands and UTF-8 rules are single-sourced in `docs/CODING_RULES.md`; `docs/AI-CODE-HEALTH.md` is compressed to a checklist and removed from the first-read full-read list (按需引用); a green-zone Fast Path (slim task confirmation + PR `Not applicable` skipping implementation blocks) lowers low-risk overhead; `scripts/validate-governance.mjs` now also checks `BUG-*`/`TD-*` duplicate IDs and relative-link reachability; `pr-packet.yml` runs the validator from the base ref; cspell covers `docs/**/*.md` and root memory files. Backend `SYBaseProject` gains a symmetric `scripts/ci/validate-governance.sh` + `verify_governance` CI job and a trimmed 120-line `PROJECT_STATE.md`. | The audit found the system mature but carrying duplicate prose, an oversized health doc inside the full-read path, an internally inconsistent modifier list, and machine gates that could be self-weakened from a PR branch and skipped docs/memory spelling. Fixing the single-source boundaries and widening the narrow guardrails removes drift risk without adding heavyweight process. | Future governance edits keep modifier/command/encoding definitions in their single source; AI-CODE-HEALTH stays an on-demand appendix; low-risk tasks use the Fast Path; backend and frontend governance guardrails stay symmetric. Backend records the symmetric decision as `DEC-20260612-002`; closes `TD-20260610-002` and `TD-20260611-001`. | If governance lint broadens to markdown structure/content quality, or a shared cross-repo governance runner replaces the per-repo validators. |
| DEC-20260612-010 | 2026-06-12 | Governance workflow executability pass (Phase E) | Acting on a second multi-dimensional audit focused on executability: `docs/AGENT_SKILL_ROUTING.md` is rewritten around skills actually installed in the current environment (Addy Osmani / Matt Pocock / repo-local sets), with Superpowers moved to an optional-not-installed section plus installed fallbacks; `docs/DYNAMIC_WORKFLOW_RULES.md` gains a mermaid decision flowchart (workflow → modifiers → worktree → red-zone escalation) and an explicit cross-repo mirror clause with the backend counterpart; filled-in examples for task confirmation / Fast Path / Workflow Packet / Memory Update Packet live in `docs/templates/workflow-packet-examples.md`; a new `docs/TESTING_RULES.md` owns test layering, co-located unit-test conventions, E2E boundaries, and backend-mock contract sync; `validate-governance.mjs` adds a 200-line soft budget for the three ledgers and link-checks the new docs; `frontend-quality.yml` runs `check:governance` so business PRs cannot silently break governance doc links; stale cspell-coverage prose in `CODING_RULES.md` is corrected. | The first optimization round fixed single-source drift inside documents; the remaining failure modes were rules that could not execute (skill names not installed), workflow routing spread across four documents with no single entry path, empty templates inviting mis-fills, no testing-strategy owner, and unbounded ledger growth. | Future skill-routing edits must verify a skill exists in the environment before recommending it; workflow semantics changes must update both repos' DYNAMIC_WORKFLOW_RULES mirror clauses; ledgers exceeding the soft budget archive resolved entries into `docs/reviews/` instead of deleting history. Backend records the symmetric decision as `DEC-20260612-003`. | If Superpowers (or another skill set) is actually installed, if the testing stack changes (Vitest/Playwright), or if ledger archival moves to a different location. |
| DEC-20260612-011 | 2026-06-12 | Governance ergonomics and confirmation protocol | Tighten the executability gaps found by the follow-up audit: red-zone work now requires a scoped confirmation protocol in `AGENTS.md`; low-risk implementation tasks may use lightweight Workflow / Loop packets instead of full-form dynamic simulation; `GIT_RULES.md` now matches the real lefthook `pre-push` `check:governance`; `PROJECT_STATE.md` no longer records stale dirty-worktree state; `UI_RULES.md` points to real Vben style/token entrypoints; `AGENTS.md` documents `codegraph` and `rtk` fallback behavior when tools are unavailable. | The remaining problems were not missing process, but rules that could mislead real execution: docs drift from hooks, red-zone confirmation without a protocol, over-heavy packet forms, stale handoff facts, a nonexistent style path, and unavailable-tool loops. | Future agents must treat red-zone confirmation as scoped and re-confirm when scope expands; use lightweight packets only for low-risk implementation tasks with no forced modifiers; determine dirty state from live `git status`; use the documented Vben style/token entrypoints; and degrade gracefully when optional tooling is absent. | If PR validation starts enforcing packet tiers automatically, if the frontend style system moves to `apps/web-ele/src/styles`, or if `rtk` / `codegraph` become guaranteed project tools. |
| DEC-20260612-012 | 2026-06-12 | PR packet tier enforcement and governance anchors | Implement the audit optimization plan by making the three execution tiers explicit in `AGENTS.md` and the PR template: Fast Path, Lightweight, and Full. `validate-pr-packet.mjs` now derives the tier from `Primary Workflow`, required modifiers, and red-zone confirmation; Full packets must include the minimum evidence sections implied by Security / DB / Backend Cross-check / Browser Verification / Red Team / red-zone signals. `validate-governance.mjs` now protects key single-source anchors across `AGENTS.md`, core rule docs, the PR template, and workflow examples. | The previous guardrail checked required fields and Red Team evidence, but did not protect the new tier model or the single-source entrypoints from future drift. Tier-aware validation catches omitted high-risk evidence while keeping Fast Path and Lightweight tasks low-noise. | Future PR template and governance-doc edits must preserve the tier names, single-source matrix, and example anchors unless the validator/tests are updated in the same change. Reviewer judgment still owns evidence quality; the validator only enforces minimum shape. | If packet tiers are renamed, if Red Team quality becomes machine-checkable, or if governance anchors move to a different entrypoint. |
| DEC-20260613-001 | 2026-06-13 | Complex task sub-agent collaboration governance | Complex tasks now use an explicit collaboration model inside existing governance: `AGENTS.md` adds a complexity decision table, `LOOP_ENGINEERING_RULES.md` defines exploration / implementation / review sub-agent templates, `DYNAMIC_WORKFLOW_RULES.md` lists default collaboration splits per Workflow, `GIT_RULES.md` clarifies worktree isolation for parallel implementation, and the PR template records sub-agent collaboration. | The project already had Workflow and Loop rules, but complex tasks needed clearer guidance on when to use sub-agents, how to bound their work, and how the main Agent should merge results without introducing a new dispatcher. | Future high-risk, cross-layer, permissions/data/report, production, or parallel implementation work must use maker/checker or bounded sub-agent collaboration while keeping the main Agent responsible for final diff, validation, Memory Update, and delivery. | If an automatic dispatcher is introduced, or if PR validation starts enforcing sub-agent collaboration evidence. |
| DEC-20260613-002 | 2026-06-13 | Governance evidence quality and memory-ledger archival | Add filled examples for high-risk Red Team / Checker evidence, sub-agent adoption records, Triage Loop output, and memory-ledger archival SOP; add a PR Packet evidence-quality checklist for human reviewers only. | The audit found that existing machine checks can verify packet shape but not evidence quality. Examples and reviewer prompts improve execution without pretending subjective judgment is automatable. Current `TECH_DEBT.md`, `KNOWN_BUGS.md`, and `DECISIONS.md` are below the 200-line soft budget, so no migration is performed now. | Future high-risk PRs should record attack paths, expected failure points, actual results, residual risks, and Checker/sub-agent adoption decisions. When memory ledgers approach the soft budget, archive only closed/resolved or long historical details into `docs/reviews/memory-ledger-archive-YYYY-MM-DD.md` while preserving root IDs and links. | If evidence quality becomes reliably machine-checkable, if ledger soft budgets change, or if memory archival moves out of `docs/reviews/`. |
| DEC-20260613-003 | 2026-06-13 | Governance and memory document placement | Core governance rule bodies now live in `docs/rules/`, and the five long-term memory bodies now live in `docs/memory/`; old `docs/*.md` rule paths and root memory paths remain as compatibility stubs only. | Separating rule bodies from long-term context makes `docs/` easier to navigate while preserving stable legacy entrypoints for existing links, PRs, and agent habits. | Future active references should use `docs/rules/...` and `docs/memory/...`; governance validation checks the new indexes, canonical anchors, compatibility stubs, and link reachability. | If compatibility stubs are removed, if the docs taxonomy changes again, or if a shared cross-repo governance layout is introduced. |
| DEC-20260613-004 | 2026-06-13 | Pathology receipt route ownership | `PathologyReceipt` is owned by the M2 specimen workflow route module at `/workflow/pathology-receipt`; M3 technical workflow keeps only entry/navigation links and root authority visibility for users with specimen-receive access. Backend menu aliases `PathologyReceipt` / `SpecimenReceipt` continue resolving to the M2 canonical route, and fallback menu routes must not declare a second M3 child with the same name/path. | Vue Router module merging concatenates route arrays without de-duplication, while access/menu generation and keep-alive wrapping depend on route names and page paths. A single owner avoids permission, tab cache, and menu ambiguity. | Future M3 work should link to `/workflow/pathology-receipt` instead of registering a duplicate page route. Route/menu tests should preserve unique route names and non-redirect component paths across M2/M3 modules. | If product reclassifies specimen receipt as an M3-owned page or a router-level alias strategy with unique names is introduced. |
| DEC-20260614-001 | 2026-06-14 | Governance compatibility entry removal | Remove the old `docs/*.md` rule compatibility entries and root memory compatibility entries; keep `AGENTS.md` as the collaboration entrypoint and use `docs/rules/` plus `docs/memory/` as the only active rule and long-term memory body locations. | The compatibility stubs made the repository root and `docs/` top level noisy after the canonical paths were already wired into `AGENTS.md`, README, and governance validation. | Future active links, checklists, PR packets, and Memory Update references must point directly to `docs/rules/...` or `docs/memory/...`; governance validation no longer requires compatibility stubs. This supersedes the compatibility-stub portion of `DEC-20260613-003`. | If external consumers require restored legacy paths or the docs taxonomy changes again. |
| DEC-20260615-001 | 2026-06-15 | Governance workflow friction reduction | Fast Path and Lightweight tasks now use minimum effective evidence: low-risk static wording, test-only, and non-layout UI micro changes can avoid Browser Verification; Summary `Validation` may carry lightweight dynamic-test evidence; Memory Update can be a one-line durable-context judgment; Loop Packet is explicit opt-in; Linear worktrees are risk-triggered rather than automatic for every implementation issue. PR packet validation mirrors this by requiring full Dynamic Tests and detailed Memory fields only for Full packets while keeping Security / DB / Production Debug / Red Team / Backend Cross-check / Browser Verification strict. | The Red Team audit found that the previous rules had lightweight intent but still forced low-risk work through full template fields, repeated validation, Memory boilerplate, Browser evidence, Loop ceremony, and worktree setup. Evidence minimums reduce task latency without weakening red-zone or high-risk controls. | Future low-risk PRs should keep packets short and substantive instead of filling empty Full blocks. Full packets remain mandatory for red-zone, security, database, production, cross-repo, permissions/data/report, Browser Verification, build/release, and Red Team signals. | If reviewers need stricter evidence for a specific low-risk class, or if PR validation starts checking evidence quality instead of only packet shape. |
| DEC-20260615-002 | 2026-06-15 | Worktree completion merge-back requirement | A task finished in an isolated worktree must be merged back into the current integration branch or the task's declared target branch before the worktree is considered complete and eligible for cleanup. The main Agent owns checking each worktree diff, resolving conflicts, excluding unrelated changes, running unified verification, and documenting the merge-back result. | Several completed worktree branches existed locally without their changes appearing on the current branch, making finished work look missing. The previous rule mentioned final merge responsibility but did not state that merge-back is required before completion. | Future worktree-based tasks cannot be delivered as complete while their accepted commits remain only on isolated worktree branches. Cleanup happens after merge-back verification, not before. | If the team adopts PR-only integration where isolated worktree branches are intentionally not merged locally. |
| DEC-20260615-003 | 2026-06-15 | Governance Phase F Phase 0 — path integrity and first-read ergonomics | Fix duplicated nested `docs/rules/` path literals in `docs/memory/PROJECT_STATE.md`; extend `validate-governance.mjs` to reject that typo pattern in active governance/memory index surfaces; replace `AGENTS.md` §1 mechanical 16-doc first-read list with a three-tier path (entry / task / foundation) executed through new `docs/rules/QUICKSTART.md`. | An external governance audit found broken path literals undermining doc trust, and the old first-read list caused context overflow without improving safety for routine tasks. A scenario-indexed quickstart preserves cross-layer guardrails while letting agents and humans read only 2–4 relevant rule docs per task. | Future first-entry and large-task sessions start with entry layer + `QUICKSTART.md` instead of reading all 16 rule files; cross-layer work still requires the collaboration foundation bundle before touching shared layers or red zones. `check:governance` fails if the nested `docs/rules/` typo reappears in scanned docs. | If the team reintroduces a full baseline read for all contributors, or if first-read routing moves into an automated dispatcher. |
| DEC-20260615-004 | 2026-06-15 | M5 material loan status query | `/operation-support/borrow` uses a single borrow-record list filtered by `loanStatus=BORROWED` or `loanStatus=RETURNED`; sibling backend `../SYBaseProject/bl-center` exposes `GET /api/v1/material-loans` for status-aware queries and keeps `/material-loans/pending` as pending-only compatibility. | Users need “待归还/归还” in one table without duplicating list surfaces, while dashboard and older pending callers must keep working. | Frontend loan filters default to `BORROWED`, refresh after return keeps the current status filter, returned rows show return metadata and no return action, and the contract reuses existing `PERM_M5_LOAN_QUERY`. Backend records the symmetric decision as `DEC-20260615-001`. | If pagination is added, status taxonomy changes, or returned-loan history requires separate authorization/audit semantics. |
| DEC-20260615-005 | 2026-06-15 | M5 material loan abnormal registration | `/operation-support/borrow` registers wax-block/slide abnormal-loan records through sibling backend `../SYBaseProject/bl-center` `POST /api/v1/material-loans/abnormal-records`; the endpoint persists to `material_loan_abnormal_records` and is protected by `PERM_M5_LOAN_ABNORMAL_REGISTER`. | The borrow page needs real异常登记落库 rather than a frontend-only dialog, and operator identity must come from authenticated backend context instead of request-body fields. | Frontend borrow tabs query material全集 from `GET /api/v1/archive-objects`, expose `借记 / 归还 / 异常登记` next to query controls, map local borrow-page status text as `BORROWED -> 借已` and `RETURNED -> 归还`, and keep abnormal registration limited to `EMBEDDING_BOX` / `SLIDE`. Backend records the symmetric decision as `DEC-20260615-002`. | If product adds specimen/white-slide borrowing, abnormal-record review workflows, or separate per-material abnormal permissions. |
| DEC-20260615-006 | 2026-06-15 | M5 physical archive batch contract | `/operation-support/archive` treats 蜡块、玻片、标本 archive tabs as multi-select batch workflows. The frontend submits `archiveCabinetId + objectIds[]` to sibling backend batch endpoints and no longer asks users to type physical object IDs in the archive dialog. | The legacy desktop flow archives selected rows into a chosen archive cabinet and lets the backend allocate positions atomically. This prevents manual ID entry errors and keeps cabinet-position allocation server-authoritative. | Frontend uses `POST /api/v1/archive/embedding-boxes/batch`, `/slides/batch`, and `/specimens/batch`; specimen batch archive may send `archiveExpiresAt` and `archiveReminderDays`. `GET /api/v1/archive-objects` rows may display `objectStatus`, sampled/sliced/content-described people, and specimen reminder fields. Backend records the symmetric decision as `DEC-20260615-003`. | If product reintroduces explicit position selection per row, adds photo/white-slide archive, or changes specimen reminder policy. |
| DEC-20260615-007 | 2026-06-15 | M5 archive cabinet node contract | `/operation-support/archive` `归档柜列表` consumes sibling backend `../SYBaseProject/bl-center` `GET/POST/PATCH /api/v1/archive-cabinet-nodes` as the source for AREA / CABINET / DRAWER nodes. New cabinet creation uses node form fields from the desktop reference: parent node, node type, code, cabinet type, capacity, remaining capacity, path location, and remarks. | The cabinet list is now a real hierarchy rather than frontend-synthesized type/cabinet/range rows, and remaining capacity must stay derived from backend positions instead of being edited manually. | Frontend create options expose only `申请单 / 蜡块 / 玻片 / 标本 / 试管`; legacy `STANDARD` remains display-compatible but is not offered for new nodes. Batch cabinet creation may send `parentId` and still creates cabinet nodes only. Backend records the symmetric decision as `DEC-20260615-004`. | If product adds a different hierarchy depth, makes remaining capacity manually adjustable, or introduces test-tube archive actions beyond cabinet type management. |
| DEC-20260615-008 | 2026-06-15 | M5 archive-page material loan shortcut | `/operation-support/archive` may expose a selected-row `借记` shortcut for `EMBEDDING_BOX` and `SLIDE` only, reusing the existing material-loan dialog and `POST /api/v1/material-loans` one-by-one submission; `/operation-support/borrow` remains the complete borrow/return/abnormal workbench. `SPECIMEN` stays archive-only and is not a material-loan type. | Archive operators need to borrow selected wax blocks/slides directly from the archive list after finding them, but the backend has no batch-loan or specimen-loan contract and the borrow page still owns return/history/abnormal workflows. | This supersedes only the “no borrow controls on archive page” part of `DEC-20260610-002`: archive-page shortcuts must keep permission gating with `PERM_M5_LOAN_CREATE`, skip non-in-storage or already-borrowed rows, and refresh archive-object rows after submission. Do not add specimen, photo, white-slide, return, or abnormal shortcuts to the archive page without new product/API decisions. | If backend adds a batch material-loan endpoint, specimen/white-slide borrowing, or product asks to merge the borrow workbench back into archive management. |
| DEC-20260615-009 | 2026-06-15 | M4 pathology report list workflow page | `/doctor-workflow/report` is now a case-scoped report list workflow page rather than a draft editor. Users query by `caseId` or `pathologyNo`, the page lists current-case report lifecycle rows through sibling backend `../SYBaseProject/bl-center` `GET /api/v1/pathology-cases/{id}/report-versions`, and top actions `审核通过 / 驳回 / 签发 / 发布` operate on one selected row by `reportId` while `打印 / 发放 / 回收` remain batch distribution actions by `versionId`. | Product removed task-ID query, report-body editing, and the separate lifecycle form from this page, but still needed one place to drive lifecycle and distribution across a case’s reports. The previous “formal-report list + lower editor” split no longer matched the desired interaction. | Frontend no longer shows `任务ID` / `报告正文` / `创建草稿` / `保存草稿` / `提交` on this page. The report list covers `DRAFT / SUBMITTED / REVIEWED / SIGNED / PUBLISHED`; draft/submitted/reviewed rows come from current `pathology_reports`, while signed/published rows also surface `report_versions` distribution fields. Backend records the symmetric contract as `DEC-20260615-005`. | If product restores inline report editing, requires lifecycle batch actions, changes list permissions, or wants a separate all-case report workbench. |
| DEC-20260615-010 | 2026-06-15 | M4 revision/consultation workbench query contract | `/doctor-workflow/report-revision` and `/doctor-workflow/consultation` now both use case-scoped `GET /api/v1/pathology-cases/{id}/diagnostic-workbench` as the only read entrypoint, with frontend-local status filtering and row-level `操作` dropdowns instead of page-header action forms. The consultation aggregate must include `consultations[].participants[]` so the participant-opinion dialog can choose from real invited participants. | Product wants the two pages to become consistent workbenches without adding new list endpoints or spreading row actions across many buttons. Returning participant detail from the existing aggregate keeps the read contract centralized and lets the frontend stop asking users to hand-type participant IDs. | Frontend `doctor-workflow` views, types, and service mappers must preserve the mixed context-row/list-row model and the `participants` detail shape on both `diagnostic-workbench` and mirrored tracking payloads. Backend records the symmetric contract as `DEC-20260615-006`. | If product later requires server-side pagination across all cases, a dedicated consultation/revision list endpoint, or a different participant-identity model. |
| DEC-20260616-001 | 2026-06-16 | M5 仪器设备管理旧系统对齐 | `/operation-resources/equipment` 按旧系统宽表契约扩展为“设备档案宽表 + 批量状态操作 + 前端导出/打印”页面：前端统一把 `equipmentCode` 展示为“资产编号”，复用现有设备详情/保养与预警能力，并通过 sibling backend `../SYBaseProject/bl-center` 的扩展设备档案字段和 `POST /api/v1/equipment-records/batch-status` 完成恢复/禁用。 | 设备页原有档案字段远少于旧系统截图，且恢复/禁用、导出、打印都依赖稳定的前后端宽表字段契约，不能只做前端占位。 | 前端设备表单、宽表列、详情展示、导出/打印工具、批量按钮与测试都必须围绕扩展字段工作；`nextMaintenanceAt` 继续只服务保养记录和预警，不并入旧系统主弹窗重点字段；导出/打印保持前端实现，不新增后端专用接口。后端记录对称决策为 `../SYBaseProject/DECISIONS.md#DEC-20260616-001`。 | If product changes the legacy field subset, adds dedicated backend export/print endpoints, or redefines batch status semantics/permissions. |
| DEC-20260616-002 | 2026-06-16 | M5 白片借记独立合同 | `/operation-support/borrow` 新增 `白片借记` tab，并通过 sibling backend `../SYBaseProject/bl-center` 独立消费 `GET /api/v1/white-slide-stocks`、`GET/POST /api/v1/white-slide-loans`、`POST /api/v1/white-slide-loans/{id}/return`。白片借记使用独立库存/借记模型与权限 `PERM_M5_WHITE_SLIDE_QUERY/CREATE/RETURN`，不复用既有 `material-loans` 的 `SLIDE` 合同，也不把 `WHITE_SLIDE` 塞进旧 `MATERIAL_TYPE_OPTIONS`。 | 用户需求要求“白片库存借出”真实能力，同时截图字段和打印需求明显不同于现有玻片借记；复用旧材料借还链路会把库存、权限、字段和打印语义混到一起。 | 前端必须保持白片独立 workspace、独立 service/types/打印工具；保存后可按“保存直接打印借阅单”走浏览器打印；读卡/配置按钮仅占位。后端记录对称决策为 `../SYBaseProject/DECISIONS.md#DEC-20260616-002`。 | If product later要求把白片并入统一材料借还中心、增加真实读卡器接入、或新增白片库存管理页面/打印网关。 |
| DEC-20260616-004 | 2026-06-16 | M6 统计报表工作台与明细合同 | `/m6/custom-analysis` 保持现有路径和 `CustomStatisticsAnalysis` route name，但作为“统计报表工作台”单入口承载工作量、质量与安全、关键质控、冰冻时效、更改报告、不合格标本、自定义统计 tab；不新增后端菜单/权限种子。Current sibling backend `../SYBaseProject/bl-center` exposes quality-detail APIs at `POST /api/v1/stat-reports/details/query` and `/details/export`; frontend must use those paths and must not use old `/api/v1/stat-report-details/*`. | 用户附件要求按医疗质量分析报表组织多页/多 tab 工作台；保留单入口和现有权限降低菜单/授权风险。Current backend code now has real detail routes, superseding the degraded no-detail assumption in `DEC-20260611-011`. | M6 statistics pages should reuse `stat-reports/query/export` plus `stat-reports/details/query/export`; report rows may render metric status, numerator/denominator, trend/breakdown, and source notes. Attachment items for technical work-number ranges, notification confirmation, and critical-value notification/reason entry are separate future Full Workflow tasks. | If product requires left-nav submenu split, new per-report permissions, or backend renames the detail route. |
| DEC-20260616-005 | 2026-06-16 | M6 工作量报表周期趋势合同 | `/m6/custom-analysis` 工作量 tab 通过同一个 `POST /api/v1/stat-reports/query` 合同传递 `periodMode=month | quarter | year`，并消费后端在 `OPERATION`/`WORKLOAD`指标行上返回的`trendPoints`。前端继续合并 `OP_CASE_VOLUME`、`OP_BILLING_AMOUNT`、`OP_PERFORMANCE_WORKLOAD`、`WL_DIAGNOSTIC_TASK_COUNT`、`WL_MEDICAL_ORDER_COUNT`，不新增工作量专用路径。 | Goal 3 需要按月/季/年对比业务量和人员工作量；复用通用报表接口和可扩展字段能满足趋势图、明细表和导出，同时避免新增临时 dashboard/workload 接口。 | 工作量查询 payload 必须保留 `periodMode`；后端 `trendPoints` 是趋势图优先数据源，缺失时前端仍按当前结果生成横向对比图；CSV 导出仍走 `POST /api/v1/stat-reports/export`。后端记录对称决策为 `../SYBaseProject/DECISIONS.md#DEC-20260616-004`。 | If product requires a dedicated workload aggregate endpoint, different period taxonomy, server-side paged detail rows for workload, or new workload permission codes. |
| DEC-20260616-006 | 2026-06-16 | M6 质量指标趋势、分布与明细合同 | `/m6/custom-analysis` 质量相关 tab 优先消费后端质量指标行上的 `trendPoints` 和 `breakdowns`，并通过 `POST /api/v1/stat-reports/details/query` / `/details/export` 打开分页明细和明细导出；前端 service 必须保留这些字段，不得把质量图表退化为纯前端占位。 | Goal 4 需要病理十三项质控指标的指标值、状态、分子/分母、口径说明、趋势、原因/状态分布和明细抽屉；后端已能从现有质量数据集生成趋势、分布和分页明细。 | `ReportChartPanel` 的趋势图优先使用 `trendPoints`，原因/状态图优先使用 `breakdowns`；明细模型继续保持非敏感字段，不新增旧 `/stat-report-details/*` 路径或临时 dashboard/detail 假接口。后端记录对称决策为 `../SYBaseProject/DECISIONS.md#DEC-20260616-005`。 | If product adds richer per-indicator detail schemas, critical-value reason source data, or a dedicated report-workbench aggregate endpoint. |
| DEC-20260616-007 | 2026-06-16 | M6 工作量导出与展示一致性 | `/m6/custom-analysis` 工作量 tab 的导出必须与页面当前合并结果一致，因此前端使用本地合并 rows 生成 CSV，而不是只导出单一 `WORKLOAD` 后端结果。质量与自定义统计继续使用后端 `stat-reports/export`。 | 工作量 tab 同时展示 `OPERATION` 和 `WORKLOAD` 指标，单独导出 `WORKLOAD` 会漏掉病例量、收费和绩效类结果，破坏“所见即所出”。 | 工作量导出文件名仍沿用统计报表命名规则，但文件内容由当前前端 merged rows 生成；其他 tab 保持后端导出路径不变。 | If backend later offers a dedicated merged-workload export contract or product changes the workbench to split display and export semantics. |
| DEC-20260616-008 | 2026-06-16 | M6 关键质控危急值只读统计合同 | `/m6/custom-analysis` 关键质控 tab 固定展示 `QC_FROZEN_PARAFFIN_MATCH_RATE`、`QC_CRITICAL_VALUE_COUNT`、`QC_CRITICAL_VALUE_REPORT_TIMELINESS_RATE`、`QC_CRITICAL_VALUE_REASON_ANALYSIS_COUNT`；新增危急值三项来自 sibling backend `../SYBaseProject/bl-center` `stat-reports/query` / `details/query` / `details/export`，以后端 `user_notifications.topic_code=CRITICAL_VALUE` 作为只读代理口径。 | Goal 5 需要危急值数量、及时率、原因分布和明细，但真实危急值通知确认、切片人原因填写属于后置跨模块能力；先用通知中心只读数据保证报表工作台可用且不伪造业务流程。 | 前端关键质控 tab 不再用不合格标本或诊断及时率替代危急值；原因/状态分布图优先选危急值原因分析 breakdowns；`UNAVAILABLE` 行仅显示“暂无明细”。Backend records the symmetric decision as `../SYBaseProject/DECISIONS.md#DEC-20260616-006`. | If Goal 9 lands real critical-value reason capture, notification confirmation SLA changes, or product requests dedicated critical-value statistics tables/permissions. |
| DEC-20260616-009 | 2026-06-16 | M6 Goal 6-8 report workbench indicators | `/m6/custom-analysis` 的冰冻时效、更改报告、不合格标本 tabs 固定消费 sibling backend `../SYBaseProject/bl-center` `V98__seed_m6_goal6_8_stat_indicators.sql` 种子的质量指标：冰冻诊断及时率/总超时/取材超时/切片超时/诊断超时，报告更改数量/医生数/修改原因/修订原因，不合格标本数量/占比/原因。 | Goal 6-8 需要可查询、可明细、可导出的独立报表页；继续复用 `stat-reports/query/export/details/query/details/export` 能保持单入口工作台稳定，同时避免把旧的冰冻石蜡符合率、报告发布天数、固定率当作这些专题的替代指标。 | 前端 tab 白名单必须使用 Goal 6-8 专用指标；后端 `sourceNote` 返回默认冰冻 SLA 30 分钟及真实字段/代理口径说明；Goal 9 的技术工位号、消息确认、危急值原因填写仍是后置跨模块 Full Workflow。Backend records the symmetric decision as `../SYBaseProject/DECISIONS.md#DEC-20260616-007`. | If product changes frozen SLA thresholds, adds real report-change reason fields, introduces dedicated unqualified-specimen quality tables, or asks to merge Goal 9 workflow changes into the report workbench. |
| DEC-20260616-003 | 2026-06-16 | M5 医疗废物管理双 Tab 与最小闭环合同 | `/operation-resources/medical-waste` 不再使用占位页，而是固定为 `人体标本` / `药物试剂` 双 Tab 的真实页面，并继续复用 `M5_RESOURCE_PAGE_AUTHORITIES`，不新增独立废物权限码。人体标本通过 sibling backend `../SYBaseProject/bl-center` `medical-waste` 合同完成列表、选项、标签预览、打印入袋、销毁；药物试剂通过同模块合同完成列表、保存即打印、单次交接。人体标本标签明细继续关联现有 grossing/label 数据，不在医疗废物模块新增独立标签主数据。 | 需求要的是可用的旧系统式医疗废物工作台，而当前前后端都没有现成契约；最小闭环需要明确页面 ownership、权限复用、标签来源和交接边界，避免未来又回退到 placeholder 或额外发散出一套独立权限/标签体系。 | 前端必须保持 route/menu/fallback/backend-menu 全部指向真实页面，medical-waste 类型/service 继续落在 `operation-support` 模块内；后端 `medical-waste` 模块与表结构成为稳定跨仓合同。药物试剂本轮只做到保存打印与一次交接，不扩展后续销毁状态流。后端记录对称决策为 `../SYBaseProject/DECISIONS.md#DEC-20260616-003`。 | If product later要求独立医疗废物权限码、复杂交接签名流程、服务器侧打印网关、或为人体标本新增专门医疗废物标签主数据。 |
| DEC-20260616-010 | 2026-06-16 | 系统用户角色展示依赖后端无前缀角色主数据 | 系统管理里的“分配角色”弹窗、用户列表角色列、角色管理页等角色名称展示继续直接使用 sibling backend `../SYBaseProject/bl-center` `/api/v1/roles` 和 `/api/v1/system-users` 返回的 `roleName`，不在前端单独剥离 `M2/M3/M4` 前缀。内置工作流角色的无前缀中文岗位名由后端主数据和 Flyway `V100__repair_builtin_workflow_role_names` 保证。 | 角色名原先来自数据库主数据而不是前端拼接；若前端单独清洗，会把显示规则散落到多个页面并与角色管理、导出、接口回显产生漂移。 | 前端新增/维护角色展示时应把后端 `roleName` 视为唯一展示源；若角色前缀需求再次变化，应优先在 backend `roles.role_name` 主数据层处理。Backend records the symmetric master-data decision as `../SYBaseProject/DECISIONS.md#DEC-20260616-008`. | If product later要求不同页面展示不同角色别名，或后端新增独立角色 displayName / i18n 字段。 |

## Update Rules

- Add decisions only when they change future behavior or prevent repeated debate.
- Include options/rationale in the `Context` and `Rationale` columns when concise.
- Do not delete decisions; mark superseded in `Impact` or add a new decision.
