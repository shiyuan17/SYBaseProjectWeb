# TECH_DEBT.md

## Purpose

Track durable frontend technical debt discovered during implementation, review, testing, or red-team analysis.

## Entry Format

| ID | Severity | Source | Impact | Suggested Action | Status |
| --- | --- | --- | --- | --- | --- |
| TD-20260608-001 | Medium | `pnpm lint` during technical specimen registration and diagnosis workstation deliveries | Full frontend lint was previously blocked by unrelated formatting issues in specimen/doctor workflow files and `unicorn/no-object-as-default-parameter` in `apps/web-ele/src/modules/technical-workflow/views/DehydrationWorkstationView.test.ts`. | No further action for this specific blocker; keep watching for recurrence in future lint runs. | Resolved |
| TD-20260608-002 | Medium | `pnpm check:type` during diagnosis workstation layout delivery | Full frontend typecheck was previously blocked by unrelated unused symbol `isVisibleInConfirmationScene` in `apps/web-ele/src/modules/specimen-workflow/composables/useSpecimenConfirmationPanel.ts`. | No further action for this specific blocker; keep watching for recurrence in future typecheck runs. | Resolved |
| TD-20260608-003 | Medium | `pnpm check:type` during tracking-list pathology number delivery | Full frontend typecheck was blocked by unrelated dirty slicing workflow errors in `technical-workflow` tests/types (`slideCount`, `combinedSlide`, `printedSlideCount`). | No further action for this specific blocker; slicing workflow type/test fixtures were reconciled during the print-before-slice delivery. | Resolved |
| TD-20260609-001 | Medium | Final `pnpm lint` rerun during slicing print-control trim delivery | Full lint was blocked by unrelated dirty-worktree formatting in `apps/web-ele/src/modules/doctor-workflow/views/DiagnosisWorkbenchView.vue`; later full lint passed on 2026-06-09. | No further action for this specific blocker; keep watching for recurrence in future lint runs. | Resolved |
| TD-20260609-002 | Medium | `pnpm lint` during slicing slide-print batch/window delivery | Full lint was blocked by unrelated dirty-worktree formatting in `apps/web-ele/src/modules/specimen-workflow/composables/useSpecimenManagementVerify.test.ts` and `typescript-eslint(no-non-null-assertion)` in `apps/web-ele/src/modules/specimen-workflow/api/specimen-workflow-mock-core.ts:132`; both are now reconciled and full lint passed on 2026-06-09. | No further action for this specific blocker; keep watching for recurrence in future lint runs. | Resolved |
| TD-20260609-003 | Medium | `pnpm check:type` during slicing slide-print batch/window delivery | Full typecheck was blocked by unrelated `TS2322: Type 'null' is not assignable to type 'string'` in `apps/web-ele/src/modules/specimen-workflow/composables/useSpecimenConfirmationPanel.test.ts:457`. | No further action for this specific blocker; `pnpm check:type` passed on 2026-06-09 during specimen outbound identifier delivery. | Resolved |
| TD-20260609-004 | Medium | Full validation during specimen outbound success-state retention fix | Full `pnpm lint` / `pnpm check:type` were blocked by unrelated dirty slicing workflow changes: `SlicingWorkstationView.vue` had a nested ternary and unused merge handlers, while slicing tests/fixtures were missing required `embeddingBoxIds`, `taskIds`, and `mergedPrintGroup` fields. | No further action for this specific blocker; slicing merge-group changes were reconciled during the pending slide-print merge delivery. | Resolved |
| TD-20260609-005 | Medium | `pnpm check:type` during slicing pending slide-print merge delivery | Full frontend typecheck was blocked by unrelated dirty dashboard code: `apps/web-ele/src/modules/dashboard/api/internal/dashboard-specimen.ts:82` mapped nullable alert titles into required dashboard alert titles. | No further action for this specific blocker; specimen dashboard alerts now normalize the title to a non-null string before mapping to `DashboardAlertItem`. | Resolved |
| TD-20260609-006 | Medium | `pnpm lint` during slicing pending slide-print merge delivery | Full frontend lint is blocked by unrelated dirty formatting in `apps/web-ele/src/modules/doctor-workflow/views/DiagnosisWorkbenchView.vue`; touched slicing files pass `oxfmt --check`. | Format or otherwise reconcile `DiagnosisWorkbenchView.vue`, then rerun `pnpm lint`. | Open |
| TD-20260610-001 | Medium | `pnpm lint` during embedding workstation direct remark edit delivery | Full frontend lint is blocked by unrelated `apps/web-ele/src/modules/technical-workflow/views/SlicingWorkstationView.vue` issues: `unicorn/no-nested-ternary` at line 238 and `order/properties-order` at line 1709. | Parenthesize or simplify the nested ternary and reorder the style properties in `SlicingWorkstationView.vue`, then rerun `pnpm lint`. | Open |
| TD-20260610-002 | High | Red-team spec review of `AGENTS.md` & `docs/*_RULES.md` (report `docs/reviews/agents-spec-redteam-review.html`, finding R-01) | 治理类规则（任务确认模板、Workflow Packet、Memory Update、Red Team、红区人工确认）无任何机器门禁；CI (`frontend-quality.yml`) 与 lefthook 都不校验 PR 正文，`cspell` 仅扫描 `**/*.{ts,tsx,vue}` 与 `**/README.md`，不覆盖 `docs/*.md` 与根目录记忆文件。规范可被静默绕过而不被拦截。 | 红区改动，待人工确认：①新增 CI job 校验 PR 正文必填 Packet 字段；②将 cspell 扩展到 `docs/**/*.md` 与记忆文件或新增 markdown lint。临时已在 `GIT_RULES.md §7` 增补「门禁覆盖矩阵」明示软约束边界。 | Open |
| TD-20260611-001 | Medium | `pnpm check:cspell` during AI skill routing documentation delivery | Full cspell currently fails on pre-existing dictionary gaps in domain/source terms such as `UNBILLED`, `nasopharyngeal`, `KRAS`, `NRAS`, `BRAF`, `gyne`, `SPKG`, `dehydrations`, `EBER`, `CGRS`, `TSRS`, `Dako`, `hematopoietic`, `blsysnew`, and `corpid`; no failures were reported in the newly added/changed skill-routing docs. | Review the reported domain terms, add legitimate terms to the project cspell dictionary, and rerun `pnpm check:cspell`. | Open |
| TD-20260611-002 | Medium | Full `pnpm lint` during M5 archive cabinet delivery | Full lint invoked repository-wide `oxfmt --check` and failed on unformatted documentation files `docs/AGENT_SKILL_ROUTING.md` and `docs/reference/agent-workflow-usage-guide.html`; touched `operation-support` source/style checks already passed. | Resolved by formatting the two documentation files and rerunning `pnpm lint` successfully on 2026-06-11. | Resolved |
| TD-20260611-003 | Medium | SID-82 browser verification in `SYBaseProjectWeb-worktrees/SID-82` | `pnpm install --frozen-lockfile` fails with `ERR_PNPM_LOCKFILE_CONFIG_MISMATCH` because the current workspace catalog metadata does not match `pnpm-lock.yaml`; without the locked Tailwind/Iconify dependencies installed, fallback Vite browser smoke cannot resolve `tw-animate-css`. | Reconcile `pnpm-workspace.yaml` catalog entries with `pnpm-lock.yaml` and rerun `pnpm install --frozen-lockfile`, then repeat authenticated or redirect-level browser smoke for `/m6/dashboard`. Do not commit lockfile shrinkage caused by `pnpm install --no-frozen-lockfile` unless dependency ownership is intentionally changed. | Open |

## Update Rules

- ID prefix is `TD-`, format `TD-YYYYMMDD-NNN` (e.g. `TD-20260608-001`), matching `AGENTS.md` section 8 and the PR Memory Update Packet.
- Add a new entry only when the debt is durable and actionable.
- Update `Status` instead of deleting resolved items.
- Reference tests, files, PRs, or backend evidence when applicable.
